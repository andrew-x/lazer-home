# Project documentation

The durable knowledge base for the PSA platform — maintained by the `librarian` subagent and trusted by future Claude Code sessions. If something here contradicts the code, the code wins: flag it and fix the doc.

## Map

| Doc | Covers |
|-----|--------|
| [development.md](./development.md) | Runbook: prerequisites, env setup, first run, the verify loop, schema workflow, synthetic-data seed (`db:seed`) |
| [architecture.md](./architecture.md) | System overview, committed tech stack, `src/` layout, auth, authz, running the DB, modified-Next.js deltas |
| [ui.md](./ui.md) | Frontend: component library, theming/tokens, route-group auth gating, app shell, error/404/loading conventions, the **home dashboard** (two bands, two time bases — and now **both bands mixed-window**: the personal **Tasks** block and the two **Pipeline** blocks, so every *block* names its own window) |
| [data-model.md](./data-model.md) | Core entities, how the five domains connect, and what's realized in code |
| [flows.md](./flows.md) | Cross-domain flows, the auth flow, + the technical request flow every mutation follows |
| [domains/crm.md](./domains/crm.md) | Companies (clients + partners), contacts, sales pipeline, and **tasks** — including the **personal task list on `/`** (the one non-parent-scoped task surface) and the domain's single ownership gate: the **assignee may always complete** their own task; the **pipeline on `/`** (funnel bands, deal value *borrowed* from the linked project's plan, `closedAt` and its shape CHECK, ungated because it reads revenue and never cost); plus the opportunity's **private Slack scoping channel** |
| [domains/projects.md](./domains/projects.md) | Projects (client engagements) + role/staffing lines (six disciplines — **a `DELIVERY` role *is* the delivery manager**, so who runs a project is derived, and **delivery-coverage gaps** are the warning that makes it worth deriving) + **budgets, the code-owned rate card (line of business × discipline) with a rate *snapshotted* onto each role, and plan margin** (cost/margin gated on `projects.viewMargin`; rates and the fixed-fee hourly-value comparator are not) + the list's **derived risk tags** (code-owned thresholds, judged in CAD) + **delivery notes** (dated write-ups with a 1–10 health rating; list health derived from the latest note, ungated) + the `/projects` list itself: a **sortable, server-ordered table under status tabs** (nulls last both ways; margin *ordering* gated like the figures) + the project's **public Slack delivery channel**. Its **portfolio** view lives in [domains/finance.md](./domains/finance.md) |
| [domains/allocations.md](./domains/allocations.md) | Staffing people onto projects over time; the `/allocations` planner view (built) + its manager-only inline notes + the per-cell **remaining-capacity meter** (cross-project load, PTO netted out, over-allocation flagged — but **not** a capacity model) + the home dashboard's **"Lazer Status"** band: the same read as a *point-in-time* org snapshot (staffed-today headcount, availability by week, upcoming/unfilled roles, cross-LoB lending) — which now also hosts a **CRM pipeline card** it deliberately doesn't explain |
| [domains/timesheets.md](./domains/timesheets.md) | Weekly time capture (built); approval + billing deferred; the **year-to-date personal utilization** behind the home dashboard's Your Status tiles |
| [domains/utilization.md](./domains/utilization.md) | The **Utilization report** (`/reporting/utilization`, built) — plan vs. actuals as two never-summed series behind a **`Planned \| Logged` basis toggle** (the off-screen series drives **deviation flags**); confirmed roles only; open page, but the whole **Logged basis is gated cohort-wide on `timesheets.edit`**; plus the table distinguishing the **three surfaces** that say "utilization" over three different windows |
| [domains/finance.md](./domains/finance.md) | The **Finance report** (`/reporting/finance`, built) — the first **portfolio** revenue/margin/blended-rate surface, gated on the existing **`projects.viewMargin`** (no new capability, no matrix change). **Everything is plan, nothing is invoiced** (`time_entries` points at a project, never a role), so there is no `Planned \| Logged` toggle; a **fixed fee is prorated by billable hours** into the window (contiguous windows partition it exactly) and the share **always divides by the whole plan, never the slice**; **one margin engine called twice**, so it can't disagree with a project's own panel; all aggregation is **server-side in both currencies**, unlike the utilization report |
| [domains/staff-profiles.md](./domains/staff-profiles.md) | People, roles, skills, compensation, availability; the **viewer-dependent** profile tab set (5–8 tabs), the read-only profile drawer (3–9), the `/staff` **org chart** view + the `/reporting/profile-completeness` table |
| [domains/performance.md](./domains/performance.md) | Peer feedback (incl. the per-person profile tab) + the three **Reporting** dashboards (`/reporting/compensation`, `/reporting/bonuses`, `/reporting/levels` — separately gated, aggregate & anonymized) + the **People management** write screens (`/people/levels`, `/people/compensation-plans`, `/people/bonus-payments`; both section indexes are redirects — the nav axis is **read vs. write**, [ADR 0055](./decisions/0055-nav-dashboards-vs-people-management.md)) + staff rating levels L0–L4 with per-role subratings + compensation change plans + **performance review notes** + **bonus payments** (`staff_bonus_payment` — dated payments, date-of-payment attribution, the type × LoB/role matrix) + **staff self-evaluations** (built); review cycles, goals, growth (proposed) |
| [domains/slack.md](./domains/slack.md) | **Slack channel links** (built) — the private **scoping** channel on an opportunity and the public **project** channel on a project: app setup + scopes, the **private-channel limitation** that shapes the whole feature (the bot only sees private channels it was `/invite`d to), the disclosure filter, why the gate is an `authorize` hook so **no capability changed**, and the create-then-archive ordering |
| [domains/permissions.md](./domains/permissions.md) | RBAC: permission model, role→permission matrix, enforcement layers, helpers — **plus everything outside the matrix**: ownership paths (now in four domains, incl. `crm.edit`'s one owner path — completing a task assigned to you), composite gates, the one *relationship* gate (review notes), and a capability *with* a full owner path + author-only writes (self-evaluations) |
| [decisions/](./decisions/) | Architecture Decision Records — the *why* |
| [guides/](./guides/) | **Runbooks** — how to set up, configure and maintain the app. Procedure, not knowledge: the click path, the value to paste, what to check, what to do when it fails. Currently [Slack setup](./guides/slack.md) |

## How these docs stay current

After any major change, the main agent dispatches the **librarian** subagent (`.claude/agents/librarian.md`) to reconcile these docs with the code. Decisions and non-obvious nuances become ADRs in [decisions/](./decisions/).

## Conventions for writing here

- Write for a future Claude session: "what do I need to know to work here safely and fast."
- Capture the **why**, not just the what.
- Keep it lean; link instead of repeating.
- Mark anything not yet built as **Status: proposed** or **planned**.
