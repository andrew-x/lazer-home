# Architecture Decision Records (ADRs)

Short records of decisions that shape the system — capturing the **why** and the alternatives considered, so future sessions don't relitigate settled choices or undo them unknowingly.

## Format

One file per decision, numbered: `NNNN-short-title.md`. Each has: **Status** (proposed / accepted / superseded), **Context**, **Decision**, **Consequences**. The librarian adds ADRs when a non-obvious choice is made.

## Log

| # | Title | Status |
|---|-------|--------|
| [0001](./0001-record-architecture-decisions.md) | Record architecture decisions | accepted |
| [0002](./0002-modified-nextjs.md) | The pinned Next.js is modified — verify against bundled docs | accepted |
| [0003](./0003-stack-selection.md) | Core stack selection (plain Postgres driver, not Neon's serverless driver) | accepted |
| [0004](./0004-action-layer.md) | Action layer: two-client composition + UserSafeActionError | accepted |
| [0005](./0005-ui-stack.md) | UI stack: shadcn on Base UI (base-nova), indigo light theme | accepted |
| [0006](./0006-google-only-auth-and-layout-gating.md) | Google-only auth + route-group/server-layout gating (not middleware) | accepted |
| [0007](./0007-staff-employment-effective-dating.md) | Split staff into durable identity + effective-dated employment | accepted |
| [0008](./0008-localhost-only-admin-area.md) | Localhost-only admin area, gated by env + host (not auth), outside `(app)` | accepted |
| [0009](./0009-pto-import-cancel-as-delete.md) | PTO import treats cancellations as deletes; re-syncs are destructive | accepted |
| [0010](./0010-actions-layer-owns-db-access.md) | The actions layer owns all DB access — reads are server-only `get<Thing>.ts`, not `'use server'` | accepted |
| [0011](./0011-category-agnostic-history-feed.md) | The profile history feed is a category-agnostic, server-merged timeline | accepted |
| [0013](./0013-resume-pdf-parse-not-store.md) | Résumé stored as text only; PDFs parsed server-side (unpdf), never persisted | accepted |
| [0014](./0014-rbac-better-auth-access-control.md) | RBAC on Better Auth native access control (single role, matrix-as-contract) | accepted |
| [0015](./0015-crm-company-over-client.md) | CRM org entity is "Company" (with `isPartner`), not "Client" | accepted |
| [0016](./0016-junction-table-and-shared-enum-conventions.md) | Junction-table + shared-enum conventions (first many-to-many) | accepted |
| [0017](./0017-project-roles-as-first-allocation-cut.md) | `project_roles` as the first cut of Allocation (simple rows, not effective-dated) | accepted |
| [0018](./0018-skills-inline-jsonb-catalogue.md) | Skills stored inline (jsonb) from a hardcoded catalogue, not a normalized table | accepted |
| [0019](./0019-project-opportunity-link.md) | Project ↔ Opportunity link: FK now on `opportunities.projectId`, `restrict` (amended: inverted, many opps → one project) | accepted |
| [0020](./0020-compensation-effective-dated-import-only.md) | Compensation as effective-dated facts on `staff_employment`; import-only, required (no carry-forward — under-specified rows skipped), view-gated | accepted |
| [0021](./0021-opportunity-pipeline-groups-and-fractional-ordering.md) | Opportunity pipeline: status groups in code + single global fractional ordering | accepted |
| [0022](./0022-contact-manager-self-reference.md) | Contact "managed by" self-referential FK; same-company invariant enforced app-side | **superseded** by [0052](./0052-contact-relationships-one-typed-junction.md) — `contacts.managerId` dropped; the same-company rule survives |
| [0023](./0023-feedback-privacy-tiers.md) | Peer feedback: privacy tiers as read-projections; giving open, review gated | accepted; **factually corrected** by [0047](./0047-feedback-reports-scoping-not-granting.md) (a reporting graph now exists) and [0049](./0049-review-notes-reporting-line-as-authorization-boundary.md) (a permission check now reads one — for review notes, not feedback) — the decision itself stands |
| [0024](./0024-opportunity-project-handoff-and-placeholder-roles.md) | Opportunity → Project handoff: delivery-stage project requirement + placeholder roles + role type (amended: link inverted, same-company enforced) | accepted |
| [0025](./0025-line-of-business-on-opportunity-and-project-not-role.md) | Line of business belongs to the opportunity & project, not the role; opportunities split to own schema file | superseded (project point) by 0033 |
| [0026](./0026-staff-manager-self-reference.md) | Staff "reports to": durable self-FK, import-resolved by email in two passes | accepted |
| [0027](./0027-timesheet-weekly-model-and-edit-window.md) | Timesheets: per-day weekly model, whole-week replace, ±1-week edit window | accepted |
| [0028](./0028-generic-responses-table-app-validated-question-ids.md) | Generic `responses` table keyed by (staff, question); question ids validated in app code, not a pgEnum | accepted |
| [0029](./0029-external-fx-rates-and-currency-normalization.md) | External FX rates (frankfurter.dev), USD-cross-rate conversion, never-throw fallback — first live external API call | accepted; its "convert on the client" default has **one sanctioned exception** — the projects *list* precomputes both currencies server-side ([0057](./0057-projects-list-margin-and-derived-flags.md)) |
| [0030](./0030-crm-timestamped-entries-notes-next-steps.md) | CRM notes & next steps as append logs: two concrete tables + shared kind enum, no per-entry ownership, scalar `nextSteps` dropped | accepted (notes); next-step half superseded by [0043](./0043-tasks-entity-replaces-crm-next-steps.md) |
| [0031](./0031-opportunity-project-planner-and-role-status.md) | Opportunity project planner: role `status` (tentative → confirmed), auto-confirm on won, weekly Gantt view | accepted; amended by [0033](./0033-line-of-business-on-role-derived-project-status.md) and [0045](./0045-project-page-as-delivery-side-role-editor.md) (role lock is planner-only) |
| [0032](./0032-staff-rating-levels-effective-dated-manager-only.md) | Staff rating levels (L0–L4): effective-dated, nullable, manager/admin-only with no self-view | accepted (data model + gating); one-page/no-tabs UI half superseded by [0044](./0044-performance-dashboards-split-by-permission.md) |
| [0033](./0033-line-of-business-on-role-derived-project-status.md) | LoB moves to the role; project status & LoB derived (not stored); one-click create-from-opportunity + delete/detach | accepted |
| [0034](./0034-company-status-derived-tags.md) | Company status as derived tags (Partner / Client / Prospect), not a stored column | accepted |
| [0035](./0035-schema-modules-by-import-boundary.md) | Schema modules by import boundary: drizzle-zod server-only, hand-written `z.object` for client-imported | accepted |
| [0036](./0036-lib-organized-by-domain-subfolders.md) | `src/lib` organized by domain subfolders | accepted |
| [0037](./0037-unit-tests-removed-except-rbac-matrix.md) | Unit tests removed, except the RBAC permission-matrix test | accepted |
| [0038](./0038-allocations-planner-pto-disclosure.md) | Allocations planner: public availability, gated leave reason | accepted |
| [0039](./0039-opportunities-list-view-and-board-column-capping.md) | Opportunities list view + board column capping (URL-driven view toggle, capped Maturing/Won/Lost) | accepted |
| [0040](./0040-allocations-planner-granularity.md) | Allocations planner: selectable day/week/month granularity (nominal rate at every zoom; week prorated, month flat) | accepted |
| [0041](./0041-allocation-notes-on-staff.md) | Allocation notes on `staff`: planner-inline, gated on static `staff.edit` (no owner path) | accepted |
| [0042](./0042-per-role-subratings-app-owned-jsonb.md) | Per-role rating subratings: app-owned jsonb on `staff_rating`, co-dated with the overall level | accepted |
| [0043](./0043-tasks-entity-replaces-crm-next-steps.md) | Tasks entity replaces CRM "next steps"; `crm_entry_kind` dropped, entry logs collapse to notes-only | accepted, **two scope cuts reversed in part** by [0065](./0065-home-personal-task-list-and-assignee-completion.md) — the deferred global "My Tasks" inbox is built (as a home-dashboard block), and `setTaskDone` now has an **owner path** (the assignee may always complete); the rejection of per-task ownership **still stands for create/update/delete**. See its *Since* section |
| [0044](./0044-performance-dashboards-split-by-permission.md) | `/performance` split into two gated dashboards (Compensation + Performance/levels); `/performance` becomes a permission-aware redirect | accepted; **routes moved** by [0055](./0055-nav-dashboards-vs-people-management.md) (`/reporting/*` + `/people/*`) — the split itself stands |
| [0045](./0045-project-page-as-delivery-side-role-editor.md) | Two role editors — deal-side (opportunity planner, tentative-only) vs. delivery-side (project page, any status); "confirmed roles are locked" narrowed to the planner | accepted; self-amended same-day — the project **Gantt** is an edit surface too (`editable` vs. `emphasized` split) and the project's **company** is editable, guarded against stranding a linked opportunity |
| [0046](./0046-compensation-change-plans-rating-writing-proposals.md) | Compensation change plans: commit writes ratings only, never `staff_employment`; snapshot + drift badge reconcile against Rippling | accepted (0020 stands) |
| [0047](./0047-feedback-reports-scoping-not-granting.md) | "Your reports" feedback list: `staff.managerId` **scopes** an already-permitted set, it never grants; `feedback.review` unchanged (0023 stands, corrected) | accepted (feedback point stands); **factually corrected** by [0049](./0049-review-notes-reporting-line-as-authorization-boundary.md) — "the reporting line never grants" is no longer codebase-wide |
| [0048](./0048-company-contact-relationships-beyond-employment.md) | Company ↔ contact relationships beyond employment: a separate link table (employer FK untouched), one row per pair, free-text description with suggestions | accepted |
| [0049](./0049-review-notes-reporting-line-as-authorization-boundary.md) | Performance review notes: `staff.managerId` becomes an authorization **boundary** (the one relationship-based gate); no capability, no matrix change; one-way sharing, no un-share | accepted — deliberately breaks [0047](./0047-feedback-reports-scoping-not-granting.md) §2 for this entity only |
| [0050](./0050-profile-peer-feedback-tab.md) | Peer feedback on the staff profile: per-person browse for `feedback.review` holders (discovery, not access), **recipient tier for self** (self branch checked first); browse-all still deferred | accepted (0023 + 0047 stand) |
| [0051](./0051-plan-editor-status-ladder-display-units-and-level-targets.md) | Plan editor: one ordered item `status` ladder replacing three booleans, display-only annual↔hourly unit toggle, code-owned level comp targets (Gap / Gap %) | accepted; **renumbered from a duplicate 0047** on 2026-07-29, then to **0051** when 0048 was taken on `main` |
| [0052](./0052-contact-relationships-one-typed-junction.md) | Contact ↔ contact links: **one typed `contact_relationships` junction** (`reports_to`/`succeeds`/`related`) replacing `contacts.managerId`; cardinality in four named **partial** unique indexes; `contacts.isActive` a real column, not derived, edited via the Edit-contact dialog's Status switch | accepted — **amended 2026-07-29**: the flag's user-facing vocabulary is **Active / Inactive**, not current/former (it covers "left the company" *and* "record no longer valid", and doesn't record which), with the words in the pure `src/lib/crm/contact-status.ts`; **supersedes [0022](./0022-contact-manager-self-reference.md)**; sibling of [0048](./0048-company-contact-relationships-beyond-employment.md) |
| [0053](./0053-project-budgets-and-margin.md) | Project budgets & margin: billing model + fee on `projects`, **one company-wide rate card in code** (no per-project rates), hours from real weekdays (not grid buckets), fixed-fee margin only at project level, new `projects.viewMargin` gate with masking in the read | accepted; **§1–2 self-reversed pre-merge** — a `project_role_rates` table was built, then dropped in favour of the code-owned card; the create-then-drop migration pair was collapsed into a single `0016` when this branch rebased onto `main`. **Renumbered from a duplicate 0052** on 2026-07-29, when 0052 was taken on `main`; **extended to the `/projects` list** by [0057](./0057-projects-list-margin-and-derived-flags.md) |
| [0054](./0054-staff-org-chart-dom-tree.md) | Staff org chart: an **indented DOM tree** with CSS pseudo-element connectors (no SVG, no library — the ui.md rule honoured, not bent); filters **prune to matches plus their ancestor chain** (`passThrough`, derived at render, never stored); "show inactive" is a prune predicate, not a pre-filter; `buildOrgForest` normalises self/orphan/cycle edges into badged roots; read-only, no migration, no new permission surface | accepted |
| [0055](./0055-nav-dashboards-vs-people-management.md) | Nav split by **read vs. write**: `Reporting` (every read-only workforce surface, `/reporting/*` — was `Analytics`, before that `Dashboards`) vs `People management` (the write screens); bonuses become their own dashboard; profile completeness moved to Reporting (2026-08-03 amendment); no gate/schema change | accepted |
| [0056](./0056-bonus-type-cross-cut-matrix.md) | Bonus type as a **cross-cut matrix** (type × LoB/role): **money-only cells** because recipients are distinct counts that don't sum, nothing rendered below two type columns, empty intersection = `null`/em dash; bonus type is a **dashboard-local** filter passed through `DashboardFilterBar`'s new `extraFilters` slot, not added to the shared `useDashboardFilters` | accepted |
| [0057](./0057-projects-list-margin-and-derived-flags.md) | Projects list: **derived risk flags** (negative / low margin, ending soon) with thresholds in code, evaluated **server-side in one currency**; plan margin **precomputed for both display currencies** (a deliberate deviation from [0029](./0029-external-fx-rates-and-currency-normalization.md)/[0053](./0053-project-budgets-and-margin.md) §8 — no per-role compensation cost on the wire for a list); request-scoped cost/FX context; badge row reserved for warnings, status + LoB demoted to card fields | accepted, **§7 superseded**; **renumbered from a duplicate 0056** on 2026-07-30, when 0056 was taken on `main` by [0056](./0056-bonus-type-cross-cut-matrix.md); **extended** by [0059](./0059-project-delivery-notes-and-list-health.md) — a fourth, **ungated** `lowHealth` tag, so "only Ending soon without `viewMargin`" no longer holds; **§7's card layout is superseded by [0061](./0061-projects-list-as-a-sortable-table.md)** (a table with status tabs), which also builds the margin sort §7 listed as unbuilt — §3, §6 and §8 (both currencies server-side, list-scoped FX note, currency as client context) stand |
| [0058](./0058-self-evaluations-dated-records-with-snapshotted-answers.md) | Staff self-evaluations: a **new dated table** (not the generic `responses` table — a periodic record needs N rows per *occasion*), each answer storing its own **section/prompt snapshot** so a record renders entirely from itself; `selfRating` a **typed column** on a **separate pg type** built from peer feedback's shared tuple; **reads own-always ⁄ `ratings.view`, writes author-only with no admin override**; edits refused once the question set moves on. **[ADR 0032](./0032-staff-rating-levels-effective-dated-manager-only.md) is not weakened** — never join `staff_rating` beside a self-rating | accepted; no matrix change; **renumbered from a duplicate 0055** on 2026-07-30, when 0055 was taken on `main` by [0055](./0055-nav-dashboards-vs-people-management.md); **amended same day** — `evaluation_date` dropped by `drizzle/0020`, records dated by `createdAt` alone; its author-only write rule is deliberately **inverted** for delivery notes by [0059](./0059-project-delivery-notes-and-list-health.md) |
| [0059](./0059-project-delivery-notes-and-list-health.md) | Project delivery notes: **health as a dated document** (`project_delivery_notes`), never a `projects.health` column — the list metric is the **latest note** by `(noteDate, createdAt)`; the 1–10 rating a **typed integer + check constraint** (not jsonb, not a pgEnum); writes on the **static `projects.edit`** capability with edit/delete **deliberately not author-only** (inverting [0058](./0058-self-evaluations-dated-records-with-snapshotted-answers.md) §5 — a shared operational record, so `authorStaffId` is attribution only and never an authorization input); a fourth `lowHealth` flag at **≤ 4**, unrated ⇒ no flag, and **visible to every viewer** unlike the margin flags | accepted; no matrix change; its **monochrome** treatment of low health is what keeps the health *bar* added by [0061](./0061-projects-list-as-a-sortable-table.md) uncoloured |
| [0060](./0060-allocations-capacity-meter.md) | Allocations planner: a **per-cell remaining-capacity meter** — PTO **nets out** of capacity (`100 − away`, superseding the old side-by-side "Away" reading), tentative counts alongside confirmed, and it runs on a **second** load formula (`bucketLoadPercent` — uncapped, prorated at *every* granularity) because summing the displayed rates would total 200% for two half-month roles; `bucketPercent`/`weekPercent` deliberately untouched, so the opportunity planner is unaffected and [0040](./0040-allocations-planner-granularity.md)'s dual-call-site rule holds. Rounded once from the raw sum; `null` on zero-working-day buckets; colour only for over-allocation | accepted; **extends 0040** (whose "still a view, doesn't sum or flag" bullet no longer holds) without superseding it; no schema, gate or matrix change ([0038](./0038-allocations-planner-pto-disclosure.md) intact — no new disclosure) |
| [0061](./0061-projects-list-as-a-sortable-table.md) | The `/projects` list redesigned from a card grid into a **sortable, paginated table** (nine columns, figures right-aligned + `tabular-nums`, a monochrome ten-segment health bar) with the five status sections as **link-based tabs** carrying filter-aware counts — which deleted the grouped-vs-filtered branch, three page params (→ one `page`) and `getProjectsInBuckets`; **sorting is server-side and URL-backed** (`sort`/`dir`) with **nulls last in both directions** and a per-column first-click direction; margin has no SQL expression so it sorts in memory over the whole bucket; **the margin *ordering* is gated by `projects.viewMargin` exactly like the figures** — a ranking is compensation-derived too | accepted; no matrix change; **supersedes [0057](./0057-projects-list-margin-and-derived-flags.md) §7** (the card layout) and resolves its "no margin sort" open item; **renumbered from a duplicate 0060** on 2026-08-02, when 0060 was independently taken on `main` by [0060](./0060-allocations-capacity-meter.md) |
| [0062](./0062-utilization-report-two-series-and-timesheet-disclosure.md) | Utilization report (`/reporting/utilization`): **two never-summed series** — planned (`project_roles`) vs confirmed (submitted `time_entries`) — every confirmed figure paired with **submitted-week coverage**; the **page is open** (the planned series only re-aggregates what the allocations planner already discloses; PTO *type* never selected) but **cross-person confirmed hours are gated on `timesheets.edit`**, scoped by a SQL predicate and withheld as `null`-not-`0`; **billable-only cohort**, membership by *employment window* not `isActive`; PTO beats a role but **over-allocation is not clamped** (the first surface that sums a person's load, answering [0040](./0040-allocations-planner-granularity.md)'s open question); tentative roles at **full weight** — no win-probability field exists, so tiers deferred to one constant | accepted; **no schema change, no matrix change** — `timesheets.view` is the named path if the audience ever widens; **renumbered from a duplicate 0060** on 2026-08-02, when 0060 was already taken on `main` by [0060](./0060-allocations-capacity-meter.md). **§1, §3, §7, §8 and §9 superseded the next day by [0064](./0064-utilization-single-basis-toggle-and-cohort-wide-logged-gate.md)** — two-series-everywhere became a basis toggle, tentative roles were dropped, LoB attribution went per-person and hours-based, the own-row logged path was removed, and the window default became period-to-date. **§2, §4, §5 and §6 stand** |
| [0063](./0063-home-dashboard-two-time-bases-and-point-in-time-staffing.md) | The home dashboard keeps **two deliberate time bases**: *Your Status* is **year to date** from submitted timesheets, *Lazer Status* is **point in time** from the `project_roles` plan (never timesheets, so thin submission can't read as an idle bench) — so **every figure must name its window** and the bare word "utilization" is banned on the page. Staffing = people holding a **confirmed** role spanning today (leave does **not** un-staff), population `isBillable`, `normalizedRate` divided by full-time headcount and **uncapped**, breakdown by the *person's* discipline, and **no small-cohort suppression** (headcounts over allocations `/allocations` already publishes by name). First client JS on `/`: filters in memory, following [0062](./0062-utilization-report-two-series-and-timesheet-disclosure.md)'s URL-bounds-the-query split — so the payload is a Client-Component prop and `buildOrgStatus` **whitelists fields, never spreads**, keeping `allocationNotes` ([0041](./0041-allocation-notes-on-staff.md)) and `skills` off the wire. `getAllocationsGrid` gained `openRoles` additively at zero query cost | accepted; **no schema change, no matrix change**; **complements 0062** (a third kind of number, not a merge) and reuses [0038](./0038-allocations-planner-pto-disclosure.md)'s PTO split unchanged; **amended 2026-08-03** by [0065](./0065-home-personal-task-list-and-assignee-completion.md) — Your Status now also carries **point-in-time** task state, so its §2 rule is satisfied on the tile hints + the Tasks caption rather than the band description (the quoted description string is stale; the rule is not) |
| [0064](./0064-utilization-single-basis-toggle-and-cohort-wide-logged-gate.md) | Utilization report v2: a **`Planned \| Logged` basis toggle** replaces two-series-everywhere (both still computed, one rendered; the other spent on **deviation flags** needing **both** a 20% relative *and* an 8h absolute gate) · **tentative roles deleted outright** — a forecast isn't an allocation and there's no win probability to weight one by · LoB alignment becomes **one row per person, hours on both bases**, with **leave taken while staffed booking to that project's practice** (the client is carrying the cost) and bench/unstaffed leave going home · **PTO and bench are full-time-only on both sides**, fixing a real bug where logged PTO/bench summed everyone against a full-time plan · `internalAdmin` dropped (no planned counterpart) · **logged access gated cohort-wide with no own-row path** (`canViewLogged` replaces `confirmedStaffIds`; the read skips both timesheet queries) — strictly tighter · **period-to-date presets** (an in-progress period ends *today*), `today` server-resolved, and ◀ ▶ arrows that step **whole calendar periods** (`shiftRange`, shape-detected — sliding by window length landed on a 3-day sliver early in a month) · the Utilization section is **six stat tiles, not a table** (one number per row isn't a table; no total tile, since project + PTO + bench reconciles by construction — `UtilizationSplitRow`/`rows`/`summary.utilization` deleted, `StatCard` gained a `marker` slot) · one bordered **filter panel** with no per-control fine print · order = **Utilization first**, then headcount/roles/bench/PTO, then the two per-person tables; the 6th section is *titled* "Staff utilization breakdown" while its code identifiers deliberately stay `staffBreakdown` · client-side search/filter/pagination on both per-person tables (`ClientPaginationControls` added beside the unchanged link-based `PaginationControls`) | accepted; **no schema change, no migration, no matrix change** — permissions.md's *narrative* moved, its matrix didn't; **supersedes [0062](./0062-utilization-report-two-series-and-timesheet-disclosure.md) §1, §3, §7, §8, §9** |
| [0065](./0065-home-personal-task-list-and-assignee-completion.md) | A **personal task list on the home dashboard** — every task assigned to you, inside the *Your Status* band, with a search/kind-filtered open list and a capped **Archive** dialog. **Partially reverses [0043](./0043-tasks-entity-replaces-crm-next-steps.md)** on two of its explicit scope cuts: the deferred global inbox is built, and `setTaskDone` gains an **owner path** — the task's **assignee may always complete it**, anyone else needs `crm.edit` (because `crm.edit` is sales/manager/admin while `ownerStaffId` can be anyone, so the list would have been read-only for exactly the people whose list it is); `createTask`/`updateTask`/`deleteTask` **keep** the flat gate, since rewording or destroying a task *is* editing CRM data. Same decision-point shape as [0014](./0014-rbac-better-auth-access-control.md)'s `canEditStaff` (pure predicate + `ActionAuthorize` hook), so **no capability and no matrix change**. Ticking a task **doesn't hide it** — a session-scoped `Map<id, done>` of overrides passed as `keepIds`, exempting those rows from the **status** filter only. Panel sorts by assignment, archive by completion (capped at 200 and says so); staleness is **open-tasks-only** at 7 days off a server-stamped `nowMs`. `MyTaskView` is a whitelist (Client Component prop on `/`) and `getMyTasks` takes **no `staffId`** — own-data-only by construction. One index, `tasks_owner_done_idx` | accepted; **no matrix change**; one index (`drizzle/0023`); **complements [0063](./0063-home-dashboard-two-time-bases-and-point-in-time-staffing.md)** — its §2 "name the window" rule now lands on the tile hints + the Tasks caption instead of the band description, unchanged in substance |
| [0066](./0066-rate-card-by-line-of-business-and-snapshotted-role-bill-rates.md) | The rate card gains a **second key** — `(lineOfBusiness, roleType)`, `Partial` in both dimensions: `DEFAULT_BILL_RATE` + `BILL_RATE_EXCEPTIONS` (ships **empty**) read only through `billRateFor`, with `BILL_RATES`/`isFlatRateCard()` **deleted** rather than redefined (the map holds deviations now, so the old name would have lied) — and **each role snapshots a rate into `project_roles.billRate`** (`numeric(12,2)`, **NOT NULL, no default**, `> 0`), editable after, so **revising the card prices only *future* roles** and a plan's revenue is reproducible from its own rows. One shared `snapshotBillRate` transform on all five role schemas fills it (its default depends on two sibling fields, so `.default()` can't); `computeProjectMargin` therefore **reads no card at all** — a `billRateFor` lookup in the math is now a bug. "Off standard rate" is **derived** (`isOffStandardRate`, rounded cents) with **no `rateIsCustom` column**, deliberately conflating *negotiated* with *stale* because stale prices are the new failure mode and only the second question is actionable. Fixed fee gains a **comparator** (`hourlyValue` + delta/percent, built from the rows' rates, **uncoloured**, rendered outside the `viewMargin` branch) while 0053 §5's ban on apportioning **stands**. `DELIVERY` becomes a sixth discipline (three lines, one `ADD VALUE`, zero UI edits) | accepted; **supersedes [0053](./0053-project-budgets-and-margin.md) §1–2 in part** (§1's `hourlyRate` rejection reversed — this is the deliberate reopening it asked for; §3–§9 stand, and 0053's "retroactive repricing" + "unrepresentable *because the map is total*" claims are now false). **No capability, matrix or RBAC change** — writes ride `projects.edit`, reads are ungated like the rest of revenue. Two migrations (`drizzle/0024`, `0025` — the latter hand-edited); `project-margin.test.ts` 22 → 35 tests. **§3's "`project_delivery_managers` is unchanged and unrelated" was overtaken the same day by [0068](./0068-delivery-managers-as-project-roles-and-coverage-gaps.md)**, which dropped the junction and made a `DELIVERY` role *be* the delivery manager |
| [0067](./0067-slack-channel-links-bot-token-denormalized-pairs-and-record-scoped-gate.md) | **Slack channel links** for opportunities and projects: **one workspace bot token, no per-user OAuth** — whose consequence is the feature's defining limitation (`groups:read` sees only private channels the app was *added to*, so scoping-channel search/suggestions are structurally incomplete, stated in the UI rather than worked around) · **denormalized column pairs, not a `slack_channel_links` table** (both relationships are 1:1; a polymorphic table needs an untyped `recordId` with no FK, losing the cascade) with a per-table named unique index + a both-null-or-both-set CHECK, and **cross-kind uniqueness as a UX rule, not a DB invariant** · **the gate follows the record being written** via `metadata.authorize`, because `crm.edit` and `projects.edit` are **disjoint** — so **no capability and no matrix change**, and `SLACK_CHANNEL_TARGETS` makes kind/table divergence unrepresentable · **no naming-convention filter on the picker** — any channel the bot can see is linkable (the ones predating the convention are the ones people need), so private channel *names* are disclosed to capability holders: accepted, bounded by `/invite` being opt-in + a required non-blank query, and turned into the operational rule "don't invite this app to sensitive private channels" · **create is not transactional and Slack has no delete**, hence the step order (invitee resolution *before* the irreversible call, `conversations.archive` as the compensating action, invites last and best-effort) · **`updateTag`, not `revalidateTag`** (the repo's first cache tags; the two-arg form's stale-while-revalidate would serve a list without the just-created channel) · the stored name is a **display snapshot**, every link is by id · the suggestion is a **client-triggered, one-record read**, never a list · off when `SLACK_BOT_TOKEN` is absent — but the row is hidden **only from viewers without the managing capability** (a holder gets a muted "Slack isn't connected"; an already-stored link renders either way) · **not seeded** | accepted; **renumbered from a duplicate 0066** on 2026-08-04, when 0066 was independently taken on `main` by [0066](./0066-rate-card-by-line-of-business-and-snapshotted-role-bill-rates.md) (its migration renumbered `drizzle/0024` → `drizzle/0026` in the same merge); **§10 amended the same day** — hiding the feature-off row from *everyone* was reversed, since an invisible feature can't be adopted or debugged by the person who'd connect it; no matrix change; `drizzle/0026`; **extends [0029](./0029-external-fx-rates-and-currency-normalization.md)** to the first *secret-bearing* integration, and `src/lib/slack/` is the `format/fx.ts` case under [0036](./0036-lib-organized-by-domain-subfolders.md); **§5 corrected 2026-08-04** — it described a `disclosableSlackChannels`/`isConventionChannelName` filter that never existed in the code (the guide was right; the ADR and domain doc were the drift) |
| [0068](./0068-delivery-managers-as-project-roles-and-coverage-gaps.md) | **`project_delivery_managers` is dropped**; a delivery manager is an ordinary `project_roles` row with `roleType = "DELIVERY"` — dated, statused, priced — so a project's delivery managers are **derived** like its status, LoBs and health already were. One pure module (`delivery-coverage.ts`) owns both "who runs this" and the thing the dateless junction structurally could not express: a **coverage gap**. Its policy, each predicate asserted *by name* in 23 tests: the **window** is min-start…max-end over live **non**-delivery roles (so oversight over-running the last engineer can't widen the window it then trivially covers, and an all-delivery project reports nothing); **live = not `cancelled` symmetrically** — matching *neither* the utilization report's confirmed-only nor the planner meter's confirmed+tentative, because those ask "whose capacity is committed" where this asks "does the plan account for delivery" (confirmed-only would read every unwon plan as 100% uncovered); an **open delivery role is a gap, not coverage** (the warning must not go quiet when nobody is accountable), and `isDeliveryRole` exists only so the sidebar can say "Open delivery role" instead of "Unassigned"; **weekends are never gap days** (a Saturday can't start *or end* a run, so a Friday→Monday handover is contiguous and both bounds are always weekdays); and **no minimum-gap threshold** — one uncovered weekday warns, with `weekdays` carried on each gap so the one-line filter is ready. ⚠️ **Delivery time now counts as money and capacity** — a `DELIVERY` role carries `hoursPerDay` + `billRate`, so it moves plan revenue/cost/margin, the capacity meter, the *Planned* utilization series, the home staffing count and load %; **existing figures shift silently** (seed gives delivery roles 1–2 h/day so a manager on three engagements isn't 300%). Surfaces: a muted (**not** destructive — colour is reserved for a loss) `DeliveryCoverageNotice` above the tabs on `/projects/[id]` showing **past gaps too**, and a third list flag `noDeliveryManager` (3rd of 5, `secondary`, pre-derived gaps in) showing **only gaps ending today or later**; **deliberately not on the opportunity Project-plan tab** (all-tentative pre-sale plans would fire on nearly every deal), with a `BudgetSummaryPanel` *uncosted-oversight* notice named as the deferred alternative. The sidebar field becomes **read-only derived** (`delivery-managers-meta.tsx`; `/projects/[id]` now has **two** inline-editable fields, not three), and the junction's write paths are **deleted**: `createProject.deliveryManagerIds`, `updateProject` → name-only, `updateProjectField`'s `deliveryManagers` variant (union now **two** variants), the DM combobox, and the "Delivery managers" summary tile + `deliveryManagerLabel`. **Query count went *down***: four junction reads gone, `getProjectsList`'s follow-ups 3 → 2 (Delivery column and coverage flag now share one role set, so they can't disagree), `getMyAllocations`' `managedProjects` deleted and `MyAllocationRow`'s hours/dates **non-nullable again** | accepted; **renumbered from a duplicate 0067** on 2026-08-04, when 0067 was independently taken on `main` by [0067](./0067-slack-channel-links-bot-token-denormalized-pairs-and-record-scoped-gate.md) (its migration renumbered `drizzle/0026` → `drizzle/0027` in the same merge); **one migration** (`drizzle/0027`, a bare `DROP TABLE … CASCADE`) with **deliberately no backfill** (five NOT NULL columns the junction couldn't supply; an invented `billRate` fabricates revenue and an invented `lineOfBusiness` changes the derived LoB set — dev data is synthetic, so it was re-seeded). **No permission, capability or matrix change** — this only *removes* mutation surface; ⚠️ unrelated to the RBAC role literally named `delivery-manager`. **Supersedes [0045](./0045-project-page-as-delivery-side-role-editor.md)** in part (its field-scoped `deliveryManagers` variant is gone) and **corrects [0066](./0066-rate-card-by-line-of-business-and-snapshotted-role-bill-rates.md) §3**. Staff-profile label changed "Delivery manager" → "Delivery"; `PROJECT_FLAGS_REVIEWED_ON` **not** bumped (no threshold added) |
| [0069](./0069-google-drive-folder-links-per-user-oauth-and-the-privacy-invariant.md) | **Google Drive folder links + Files tabs** — [0067](./0067-slack-channel-links-bot-token-denormalized-pairs-and-record-scoped-gate.md)'s shape (two kinds, denormalized column pairs, a record-scoped `authorize` gate) with two deliberate reversals. Leads with **the privacy invariant, enforced structurally**: `driveList` **hardcodes** `corpora=drive` + `driveId=GOOGLE_DRIVE_ROOT_ID` so scope is *not a parameter* and no call site can widen a listing to a personal Drive; the only path touching a personal file is `copyDriveFile`, whose id comes from a **Picker click**; and it **copies, never moves** (nothing rewrites `parents`) · **per-user OAuth riding the Google login** — the repo's **first** (Slack is one bot token), so Google enforces shared-drive membership and Drive's audit trail names the real human; `accessType: "offline"` + `prompt: "…consent"` are load-bearing (no refresh token otherwise), `encryptOAuthTokens` switched on **while it's still free**, and all token access goes through `auth.api.getAccessToken` (a Drizzle read now returns ciphertext) · **the full `drive` scope, not `drive.file`** (per-file access makes a folder listing structurally incomplete), usable unverified **only because the consent screen is Internal** · **existing users must sign out and back in once** — the scope grants nothing retroactively; `linkSocial` incremental consent is the non-destructive fix · **zero cache tags, as a security decision** — the exact opposite of 0067 §7, because a per-user token in a shared cache entry is cross-user disclosure · **the Picker instead of an uploader**, conceding a full-Drive token in the browser (`setOAuthToken` is the only way), as **two single-view Pickers** ("Upload" / "From my Drive") since a combined one returns an undifferentiated PICKED list and inferring wrong duplicates files — **mode, not payload, decides** · `crm.edit` \| `projects.edit` resolved from the kind, **no capability and no matrix change**; browse/copy/search/picker-token carry **no** capability deliberately (they run on the viewer's token, so a gate would be theatre), and `getDrivePickerToken` takes `z.object({})` — a `userId` there would be a vulnerability · **`files.delete` as an EXACT compensating action** (better than Slack's archive-only) plus a name-collision precheck that **refuses** rather than duplicating · stored name is a **display snapshot**, no rename sync · one 1000-row page, `truncated` surfaced rather than swallowed · a `ok`/`reconnect`/`no-access`/`not-configured`/`unavailable` status envelope that never throws · **not seeded**, and dev points at a **separate shared drive** instead of a `test-` prefix | accepted; **renumbered from a duplicate 0068** on 2026-08-04, when 0068 was independently taken on `main` by [0068](./0068-delivery-managers-as-project-roles-and-coverage-gaps.md) (its migration renumbered `drizzle/0027` → `drizzle/0028` in the same merge); **no capability, matrix or RBAC change** (`permissions.ts`, its test and [permissions.md](../domains/permissions.md)'s matrix verified untouched); `drizzle/0028` (4 ADD COLUMN, 2 unique indexes, 2 checks, no backfill); **no `auth:generate`** — `account` already had every column; **reverses [0067](./0067-slack-channel-links-bot-token-denormalized-pairs-and-record-scoped-gate.md) §1 and §7 with reasons**; extends [0029](./0029-external-fx-rates-and-currency-normalization.md) to a *per-user-credentialed* service; `src/lib/drive/` is the [0036](./0036-lib-organized-by-domain-subfolders.md) case again. The planned similarity-based folder **suggestion was dropped** (and with it the `src/lib/core/similarity.ts` extraction — `src/lib/slack/channel.ts` is untouched) |

> **0012 is intentionally absent** (the log jumps 0011 → 0013). It was a short-lived
> ADR documenting the *open staff-edit gap pending RBAC*; it was **withdrawn** once
> [ADR 0014](./0014-rbac-better-auth-access-control.md) closed that gap, and its file
> was removed. The number is retired — don't reuse it.

> **The plan-editor ADR has moved twice.** Filed as a duplicate **0047** on
> 2026-07-28 (two ADRs, same day, same number), it was renumbered to **0048** on
> 2026-07-29 — which collided again when `main` independently landed
> [0048](./0048-company-contact-relationships-beyond-employment.md) for the CRM
> relationship table. It now lives at **[0051](./0051-plan-editor-status-ladder-display-units-and-level-targets.md)**;
> the trunk number won. Nothing outside `/docs` linked to either old number.
> **Before adding an ADR, take the next number from this table, not from `ls`** — a
> file that isn't in the log is exactly how the first collision happened, and a
> number claimed on a long-lived branch is how the second one did.
