# Architecture

**Status: actively built out on the scaffolded stack.** The core technical stack and architectural patterns are committed and in code. What exists is the foundation (env, db, auth, action layer), the **authenticated UI shell + auth screens** (see [ui.md](./ui.md)), the **staff schema + post-login staff-record gate** (the first real data-model slice; the earlier `StaffProfileForm`/`updateStaffProfile` demo has been deleted), and a growing **CRM slice** (`companies`/`contacts`/`opportunities` data, reads, create actions, and `/companies` + `/opportunities` pages — see [domains/crm.md](./domains/crm.md)). The first real data-backed authenticated pages are **`/profile`** ("My profile") and the **browse-staff** feature — a directory (`/staff`) and per-person profiles (`/staff/[id]`) that show *other* people via the same shared `ProfileView` (see [ui.md](./ui.md)) — the first place all three call-site patterns from ADR 0010 appear together (server-only read, next-safe-action mutation, presentational client UI). Browse-staff is also the first **cross-person** data access: its reads aren't ownership-scoped, and its link/intro/resume edits are now gated by RBAC (own → always; other → the `staff.edit` permission) — see Authorization, [domains/permissions.md](./domains/permissions.md), and [ADR 0014](./decisions/0014-rbac-better-auth-access-control.md). The resume edit is a **two-step parse-then-store** flow (upload a PDF → server extracts text for review → user saves), which never persists the file — see [ADR 0013](./decisions/0013-resume-pdf-parse-not-store.md). The profile has since been **redesigned into a two-column, tabbed layout** (#33) with dedicated sections (skills, compensation, PTO, projects, history timeline, and the **Manual of Me**). That last slice rides a **generic `responses` survey store** (`responses-schema.ts`, `getManualOfMe`/`upsertResponse`, `src/lib/staff/manual-of-me.ts`) — a `(staffId, questionId)` upsert store whose question ids are app-validated (no migration per new survey); its first and only consumer is the seven free-text Manual-of-Me questions — see [ADR 0028](./decisions/0028-generic-responses-table-app-validated-question-ids.md) and [data-model.md](./data-model.md). The CRM slice adds the first **server-side-paginated** reads (`src/lib/core/pagination.ts`) and the first type-ahead search (`src/lib/core/search.ts`), plus the first flat (non-ownership) write gate: a single `crm.edit` capability gates all CRM writes (companies, contacts *and* opportunities), while reads stay open to any signed-in user. Opportunities also bring the repo's first **many-to-many junction tables** (see [data-model.md](./data-model.md#junction-tables--the-first-many-to-many-pattern)) and the first CRM ↔ staff link. A **Projects slice** (`projects`/`project_delivery_managers`/`project_roles` data, a server-paginated read, a `projects.edit`-gated create, and the `/projects` page — see [domains/projects.md](./domains/projects.md)) follows next: it reuses the junction/shared-enum conventions, adds the first **data-carrying junction** (`project_roles`, the first cut of Allocation — [ADR 0017](./decisions/0017-project-roles-as-first-allocation-cut.md)), a second flat write capability (`projects.edit`), and shares its staff/company search query bodies with CRM via `src/actions/shared/entitySearch.ts`. The first **Performance** slice — **peer feedback** — has also shipped: a `feedback` table (`performance-schema.ts`), give/read actions, and a `/feedback` page, with **three privacy tiers enforced in the read projections** (giving is open to any active staff, recipients see a limited view, `feedback.review` sees everything) — see [domains/performance.md](./domains/performance.md), [data-model.md](./data-model.md), and [ADR 0023](./decisions/0023-feedback-privacy-tiers.md). That page has since gained a reviewer-only third tab, **"Your reports"**, which is the repo's first read **scoped by the `staff.managerId` reporting line** — on the *unchanged* `feedback.review` gate, so the relationship only narrows an already-permitted set and never grants access ([ADR 0047](./decisions/0047-feedback-reports-scoping-not-granting.md)). A **Timesheets slice** has also shipped: `timesheets`/`time_entries` (`timesheets-schema.ts`), the `/timesheets` weekly grid, and save/submit/reopen actions — the first **actuals** capture (per-day rows, whole-week transactional replace, submit-locks-the-week, a ±1-week owner edit window with the `timesheets.edit` capability bypassing it). No approval or billing yet — see [domains/timesheets.md](./domains/timesheets.md) and [ADR 0027](./decisions/0027-timesheet-weekly-model-and-edit-window.md). The Performance domain has grown a second slice — a **compensation & headcount analytics dashboard** (now `/dashboards/compensation`): an anonymized aggregate read (`getCompensationSummaryData`) over the latest `staff_employment` comp per active person, with role breakdowns, line-of-business/employment-type/role filters, and a CAD/USD currency toggle. It's gated by the **existing** `staff.viewCompensation` capability (no matrix change), and introduces two firsts: the repo's **first live external HTTP call** — FX rates from frankfurter.dev, normalized via the pure `src/lib/format/fx.ts`, with a never-throw stale fallback ([ADR 0029](./decisions/0029-external-fx-rates-and-currency-normalization.md)) — and a **permission-aware sidebar** (nav items carry an optional `permission`, hidden from users who lack it via `visibleNavHrefs`). See [domains/performance.md](./domains/performance.md) and [ui.md](./ui.md). A **third Performance slice — staff rating levels (L0–L4)** — then landed: a new **effective-dated `staff_rating` table** (nullable level, DB `CHECK` 0–4; same history-as-rows model as `staff_employment`, [ADR 0007](./decisions/0007-staff-employment-effective-dating.md)), surfaced as its own **Levels dashboard** at `/dashboards/levels` (managers/admins only) with a level-distribution SVG bar chart, average-level-by-role, and per-role subrating averages — **no money**; the comp/rate-**by-level** breakdown (which reuses the comp aggregation + FX) sits on the Compensation dashboard instead, where it renders only for a viewer who also holds `ratings.view`. It adds the **first `ratings` capability** — `ratings.view`/`ratings.edit`, **manager/admin only with no owner-visible path** (stricter than compensation; finance is excluded) — see [ADR 0032](./decisions/0032-staff-rating-levels-effective-dated-manager-only.md). The two analytics views **were one merged page** (`/performance`) and were **split into separately-gated routes**, with the section index demoted to a permission-aware redirect — the route structure, not a conditional prop, now carries the permission boundary ([ADR 0044](./decisions/0044-performance-dashboards-split-by-permission.md)). Most recently, an **Allocations planner** has shipped (`/allocations`): a read-only day/week/month grid (rows = active staff, columns = time buckets) computed from `project_roles` + PTO by the pure `src/lib/allocations/allocations-grid.ts` (`getAllocationsGrid`), plus **manager-only allocation notes** on each staff row (`updateStaffAllocationNotes`, `staff.edit`-gated on both the write and the read). This is the first real consumer of the `project_roles` data-carrying junction beyond the project plan — see [domains/allocations.md](./domains/allocations.md). A **fourth Performance slice — compensation change plans** — then landed (`/people/compensation-plans`): two new tables (`compensation_plan` / `compensation_plan_item`) holding a named, effective-dated **proposal** over a cohort, where **committing writes `staff_rating` rows and deliberately never `staff_employment`** — Rippling stays the sole writer of pay ([ADR 0020](./decisions/0020-compensation-effective-dated-import-only.md) stands), and commit instead freezes a comp snapshot so the plan shows a stable before/after and badges rows whose change hasn't landed upstream. Three firsts: the repo's first **composite permission gate** (`staff.viewCompensation` **AND** `ratings.edit`, a genuine conjunction, no matrix change) and its first deliberately **identity-bearing** comp surface; the first **expandable table rows**; and a **generic autosave engine** (`src/hooks/use-autosave-queue.ts`) extracted from the profile surveys. See [domains/performance.md](./domains/performance.md) and [ADR 0046](./decisions/0046-compensation-change-plans-rating-writing-proposals.md); a follow-up round replaced the item's three workflow booleans with one ordered **status ladder**, added a display-only **annual↔hourly** unit toggle and code-owned **level comp targets** ([ADR 0051](./decisions/0051-plan-editor-status-ladder-display-units-and-level-targets.md)). Most recently a **fifth Performance slice — performance review notes** — landed: a new `performance_review_note` table (`drizzle/0013_simple_leader.sql`) holding a manager's dated write-up of a review conversation, with a `DRAFT → SHARED` one-way lifecycle. **It is the architectural outlier to flag:** it is the first and only place where **authorization reads the reporting line** (`staff.managerId`), deliberately breaking the invariant [ADR 0047](./decisions/0047-feedback-reports-scoping-not-granting.md) had stated — the decision, its costs, and the rejected role-capability alternative are in [ADR 0049](./decisions/0049-review-notes-reporting-line-as-authorization-boundary.md), and the model is in [domains/permissions.md](./domains/permissions.md). It ships with no route of its own: it's a **tab on the staff profile** alongside a new per-person **Peer feedback** tab ([ADR 0050](./decisions/0050-profile-peer-feedback-tab.md)), which makes the **profile tab set viewer-dependent**, and both also render inside a new read-only **staff profile drawer** (the app's second `Sheet` consumer) opened from a compensation-plan row. Most recently the **Projects domain gained its commercial layer — budgets & margin** (`drizzle/0016_violet_whistler.sql`): a billing model on `projects` — a fixed fee (amount + currency, guarded by a `projects_budget_shape` CHECK) or time & materials, which stores **nothing** because it bills at the company's **single per-discipline rate card in code** (`src/lib/projects/bill-rates.ts`) — and plan **revenue/cost/margin** computed by the pure `src/lib/projects/project-margin.ts` over the project's roles. (A per-project `project_role_rates` table was built and then dropped before shipping: a rate card is central policy, and per-project copies invite projects silently disagreeing about what an engineer-hour is worth. Its create-then-drop migration pair was collapsed into the single `0016` above when this branch merged `main`, so **no migration mentions the table** — the ADR is the only record it was tried.) **Two firsts to know about:** it is the first time the projects domain reads **`staff_employment`** (a role's cost *is* its assignee's pay restated hourly), which is why it adds a **new read capability `projects.viewMargin`** — granted to admin/manager/finance/delivery-manager, **not** sales — with the masking done **inside the reads** (`getProjectCostBasis` returns `null` before querying comp, because both plan readers ship to client components and `loadOpportunityPlan` is only `crm.edit`-gated); and it is the second money surface to need **FX display**, which it handles by stating the conversion **once per panel, naming the rates used** (`ProjectMargin.convertedFrom` → the new `src/components/fx-rate-note.tsx`, rendered beside the currency toggle) rather than marking each converted figure — a per-value provenance system was built and then **removed**, because "this was converted" without the rate isn't actionable and per-figure icons turn to noise past the first one. `fx.ts` is therefore unchanged: still just `AED_PER_USD` + `FALLBACK_USD_RATES` + `convert()`. See [domains/projects.md](./domains/projects.md#budget--margin) and [ADR 0053](./decisions/0053-project-budgets-and-margin.md). Most recently the performance surfaces were **regrouped in the nav**: the read-only aggregate dashboards moved under a **Dashboards** section (`/dashboards/{compensation,bonuses,levels}`) and the identity-bearing write screens under **People management** (`/people/{levels,compensation-plans,bonus-payments}`), each section a permission-aware redirect over its own children; the **bonus breakdown also became its own dashboard** rather than a section of the Compensation page, since bonuses include people who have since left and so reconcile with nothing above them. No gate, read or schema changed — see [ADR 0055](./decisions/0055-nav-dashboards-vs-people-management.md). The **sixth Performance slice — bonus payments** (`drizzle/0018_flippant_chameleon.sql`) is what that dashboard reports on: a **`staff_bonus_payment`** table of **dated payments** (sibling of `staff_pto`, not effective-dated) which **replaced `staff_employment.discretionaryBonus`** — dropped in the same migration, because a one-off bonus is an event, not a go-forward term, and one column had room for only one payment. It carries **no line of business or role**: the dashboard derives them from the employment row in force **on the payment date** (`employmentAsOf`), so last year's totals never silently rewrite themselves; and the read is deliberately **not active-staff-only** (a bonus paid to someone who has since left was still spent), which is exactly why bonuses reconcile with nothing on the Compensation dashboard. Its gates are two named constants over the **existing** matrix — `BONUS_PAYMENT_READ_ACCESS` (`staff.viewCompensation`) and the conjunction `BONUS_PAYMENT_WRITE_ACCESS` (`staff.edit` AND `staff.viewCompensation`), so finance reads totals but writes nothing. Most recently the dashboard gained a **type × line-of-business/role matrix** (`computeBonusMatrix` + `bonus-type-matrix.tsx`) whose cells show **money only** — recipient counts are *distinct* counts, so per-cell counts would look like an arithmetic error — plus a **page-local bonus-type filter** passed into the shared filter bar through a new `extraFilters` slot rather than into `useDashboardFilters`, which two other dashboards share ([ADR 0056](./decisions/0056-bonus-type-cross-cut-matrix.md)). Most recently the **`/projects` list gained plan margin and derived risk tags** (Negative margin / Low margin / Ending soon, thresholds as code-as-policy in the new pure `src/lib/projects/project-flags.ts`) — no schema change, but two nuances worth knowing: the flags are **always evaluated server-side in CAD** so a tag can't follow the display toggle, and the list **precomputes margin in both display currencies server-side** rather than shipping native amounts + rates for the client to convert (the detail page's contract, unchanged) precisely so **no individual's compensation-derived hourly cost reaches the browser for a surface with no per-role table**; the shared cost/FX inputs are request-scoped through a React `cache()`d `getProjectsMarginContext`, still gated in the one place (`getProjectCostBasis`). See [ADR 0057](./decisions/0057-projects-list-margin-and-derived-flags.md). Most recently a further Performance slice — **staff self-evaluations** — landed (`drizzle/0019_yummy_grandmaster.sql`): a new `staff_self_evaluation` table holding a **periodic, dated questionnaire a person fills out about themselves** (seven free-text prompts + one self-rating), the first-person counterpart to feedback / review notes / assigned levels. **Three things to know:** it is a **new table rather than the generic `responses` store**, because `responses` is a `(staffId, questionId)` upsert holding one *current* answer per question while a periodic record needs N rows per **occasion** ([ADR 0028](./decisions/0028-generic-responses-table-app-validated-question-ids.md) is unchanged for profile surveys); each answer stores its own **section/prompt snapshot**, so a record renders entirely from itself and rewording or retiring a question never rewrites history; and it introduces a **fourth gate shape** — a capability (`ratings.view`) *with* a full owner path for reads, plus an **author-only write gate with no admin override** — which is why it must never join `staff_rating` or show an assigned level beside a self-rating ([ADR 0032](./decisions/0032-staff-rating-levels-effective-dated-manager-only.md) stands). No route of its own: a **Self-evaluations tab** on the profile (now 5–8 tabs) and in the profile drawer (now 3–9), read-only in the drawer. See [domains/performance.md](./domains/performance.md) and [ADR 0058](./decisions/0058-self-evaluations-dated-records-with-snapshotted-answers.md). Most recently the **staff domain** added a **profile-completeness table** (`/people/profile-completeness`): a read-only row per staff member showing which self-service profile artefacts they've filled in (links, résumé, skills, client intro, both surveys) plus two "last updated" dates — the first reader of `clientIntroUpdatedAt` and of the **one new column**, `staff.skillsUpdatedAt` (`drizzle/0021_green_the_call.sql`, stamped explicitly by `updateStaffSkills`, never `$onUpdate`). **No new capability** — it reuses `staff.edit` behind the named constant `PROFILE_COMPLETENESS_ACCESS`, and the read ships **presence, not content** (presence/counts computed in SQL). It also promoted the shared `DataTable` out of `admin/` and gave it optional sorting, and unified "has this survey question been answered" into one predicate (`src/lib/staff/survey-answers.ts`) after finding the profile view and `getWaysOfWorking` disagreeing. See [domains/staff-profiles.md](./domains/staff-profiles.md). Still pending: allocation capacity / over-allocation modelling, timesheet approval/billing, forecast-vs-actual reconciliation (the margin above costs the *plan*, not logged time), and the rest of performance (review cycles, goals, growth).

## What we're building

A professional services automation (PSA) platform for a software consultancy — one system spanning CRM, allocations, timesheets, staff profiles, and performance management. See [data-model.md](./data-model.md) for how the domains connect.

## Tech stack

| Concern | Choice | Notes |
|---------|--------|-------|
| Runtime | **Bun** | auto-loads `.env`; runs scripts and the drizzle-kit / better-auth CLIs |
| Framework | **Next.js 16** (a **modified** build — see [decisions/0002](./decisions/0002-modified-nextjs.md), `.claude/rules/nextjs.md`, `node_modules/next/dist/docs/`) | App Router |
| UI runtime | **React 19** (React Compiler enabled) | |
| Language | **TypeScript** | |
| Lint/format | **Biome** | `bun run lint` / `bun run format` |
| Database | **Postgres** via **postgres-js** driver (`postgres` pkg) | driver choice is postgres-js, not Neon's serverless driver — `.env` points `DATABASE_URL` at a **Neon**-hosted Postgres (works over the standard endpoint), which is migrated and up to date. Dev runs against a remote Postgres (no local DB / Docker); `.env.example` ships a remote placeholder. See [decisions/0003](./decisions/0003-stack-selection.md) |
| ORM | **Drizzle** (`drizzle-orm` + `drizzle-kit`), `casing: "snake_case"` | singleton in `src/lib/db/db.ts` |
| Auth | **better-auth** — admin plugin; **Google-only** (email/password disabled) | `src/lib/auth/auth.ts`, [0006](./decisions/0006-google-only-auth-and-layout-gating.md) |
| UI | **shadcn on Base UI** (`base-nova`), **Tabler icons** (`@tabler/icons-react`), Geist; light mode only, flat + mostly-monochrome (indigo used sparingly) | `src/components/ui/**`, `components.json` — see [ui.md](./ui.md), [0005](./decisions/0005-ui-stack.md) |
| Action layer | **next-safe-action v8** | `src/lib/core/action.ts` — see [decisions/0004](./decisions/0004-action-layer.md) |
| Forms | **react-hook-form** + `@hookform/resolvers` + **zod v4** | `.claude/rules/forms.md` |
| Validation | **zod v4**; schema style **by import boundary** — **drizzle-zod** (schema from the table) for server-only schemas, hand-written `z.object` (drizzle-free) for any schema a client component imports | keeps Drizzle out of the client bundle — see [decisions/0035](./decisions/0035-schema-modules-by-import-boundary.md) |
| Styling | **Tailwind v4** + `clsx` + `tailwind-merge` (`cn` helper) | `src/lib/core/utils.ts` |
| IDs | app-minted CUID2 with prefix (`@paralleldrive/cuid2`) | `src/lib/db/ids.ts` |
| Tables / CSV | **@tanstack/react-table** (+ shadcn `table`/`badge`), **papaparse**, **unpdf** (PDF text extraction, `parseResumePdf`) | tables added for the admin importers; `src/components/data-table.tsx` (read-only, optional sorting; re-exported from `admin/data-table.tsx` for the importers' existing call sites) + `admin/editable-table.tsx` + shared `admin/csv-import.tsx` are the reusable wrappers |
| Drag & drop | **@dnd-kit** (`@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities`) | powers the opportunities kanban drag-reorder — see [ADR 0021](./decisions/0021-opportunity-pipeline-groups-and-fractional-ordering.md), [ui.md](./ui.md) |

## `src/` layout

```
src/
  env.ts                     zod-validated server env (see "Env" below)
  app/
    layout.tsx               root layout (fonts, metadata, TooltipProvider, Toaster)
    (app)/                   AUTHENTICATED route group — layout.tsx gates via getCurrentUser() + getCurrentStaffAccess()→redirect, renders AppShell
      layout.tsx page.tsx profile/page.tsx settings/page.tsx loading.tsx error.tsx (segment error boundary — renders INSIDE AppShell so the shell survives; uses unstable_retry)
      staff/page.tsx staff/[id]/page.tsx  companies/page.tsx contacts/page.tsx opportunities/page.tsx projects/page.tsx allocations/page.tsx
      feedback/page.tsx feedback/new/page.tsx feedback/[id]/page.tsx  ("Peer Feedback" — a list whose THIRD tab ("Your reports") renders only for feedback.review holders with direct reports, plus the give-feedback page and the detail page; route names unchanged, only the label/title say "Peer Feedback")
      dashboards/page.tsx  (NOT a page — a permission-aware REDIRECT: staff.viewCompensation → /dashboards/compensation, else ratings.view → /dashboards/levels, else → /dashboards/utilization (the fallback since the ungated report landed; it no longer notFound()s). Compensation stays the LANDING page for anyone who can see it, so the section doesn't change under its heaviest users. The sidebar's parent nav entry points here and is itself now UNGATED ([ADR 0044](./decisions/0044-performance-dashboards-split-by-permission.md), [ADR 0055](./decisions/0055-nav-dashboards-vs-people-management.md), [ADR 0060](./decisions/0060-utilization-report-two-series-and-timesheet-disclosure.md)))
      dashboards/utilization/page.tsx  (Utilization report — UNGATED beyond the (app) session check; parses the window off searchParams via lib/utilization/utilization-range.ts, then one getUtilizationReport(range) call SSR'd into the client shell. The sensitive half — other people's confirmed hours — is withheld INSIDE the read, never serialised to a client that merely hides it — see [domains/utilization.md](./domains/utilization.md))
      dashboards/compensation/page.tsx  (Compensation dashboard — comp & headcount by role AND by level; notFound()s non-viewCompensation users; fetches getRatingsSummaryData ONLY for ratings.view holders → optional ratingRecords prop, so the by-level table needs BOTH capabilities and finance just doesn't get it. Takes NO searchParams)
      dashboards/bonuses/page.tsx  (Bonus dashboard — one-off bonus payments for ONE calendar year by line of business, role and type, PLUS a type × LoB/role matrix and a page-local bonus-type filter ([ADR 0056](./decisions/0056-bonus-type-cross-cut-matrix.md)); same staff.viewCompensation gate (BONUS_PAYMENT_READ_ACCESS); reads the year from the `year` searchParam, so each year is its own read; deliberately NOT active-staff-only, so it doesn't reconcile per-head with the Compensation dashboard)
      dashboards/levels/page.tsx  (Levels dashboard — staff level L0–L4 analytics, NO money/FX at all; notFound()s non-ratings.view users (finance excluded))
      people/page.tsx  (NOT a page — the People-management section's permission-aware REDIRECT: ratings.edit → /people/levels, else COMPENSATION_PLAN_ACCESS → /people/compensation-plans, else BONUS_PAYMENT_WRITE_ACCESS → /people/bonus-payments, else PROFILE_COMPLETENESS_ACCESS → /people/profile-completeness, else notFound(). All four branches are {manager, admin} today; the ladder stays written out, and new branches go LAST so nobody's landing page moves)
      people/levels/page.tsx  (staff levels L0–L4 edit table — notFound() non-ratings.edit users)
      people/bonus-payments/page.tsx  (bonus payment ENTRY screen — record/correct/remove payments; the CONJUNCTION staff.edit AND staff.viewCompensation (BONUS_PAYMENT_WRITE_ACCESS), so finance reads the dashboard totals but writes nothing)
      people/profile-completeness/page.tsx  (READ-ONLY sortable table of who has filled in which profile artefacts — links/résumé/skills/client intro/the two surveys + the two "last updated" dates; gated on the EXISTING staff.edit capability as PROFILE_COMPLETENESS_ACCESS (no matrix change), notFound() otherwise, and getProfileCompleteness re-checks it. A STAFF-domain screen sitting in the People-management section — see [domains/staff-profiles.md](./domains/staff-profiles.md))
      people/compensation-plans/page.tsx + [planId]/page.tsx + [planId]/staff/page.tsx  (compensation change plans — list + editor + the membership roster ("Plan staff": who this round covers, deliberately NOT in the editor); all three gated on the CONJUNCTION staff.viewCompensation AND ratings.edit, notFound() otherwise, and the editor's generateMetadata withholds the plan name from unauthorized users; [ADR 0046](./decisions/0046-compensation-change-plans-rating-writing-proposals.md))
    (onboarding)/            POST-LOGIN block screen — authed users without a usable staff record (no group layout; page self-gates)
      profile-setup/page.tsx
    (auth)/login/page.tsx    PUBLIC route group (Google sign-in)
    admin/                   LOCALHOST-ONLY tooling — OUTSIDE (app) on purpose; layout 404s non-loopback requests
      layout.tsx page.tsx loading.tsx error.tsx upload-staff/page.tsx upload-pto/page.tsx bulk-edit-roles/page.tsx manage-users/page.tsx
    error.tsx not-found.tsx global-error.tsx   error/404 conventions (Next 16 unstable_retry — see ui.md)
    api/auth/[...all]/route.ts  better-auth catch-all (mounts the whole auth API)
  actions/<domain>/          the single entry point for ALL DB access (ADR 0010)
    staff/getStaffProfile.ts staff/getStaffHistory.ts staff/getStaffPto.ts staff/getStaffAvatar.ts staff/getStaffProjects.ts  per-person reads by id (server-only get<Thing>.ts), NOT ownership-scoped (getStaffProjects backs the profile's Projects section — the person's project_roles + delivery-manager memberships)
    staff/getStaffDirectory.ts  directory read: latest-employment-per-staff (two queries, no N+1) + filter options
    staff/getCurrentStaffId.ts  session→staff id (React.cache, wraps ownStaffId); getMy* wrappers delegate to the getStaff* cores
    staff/ownStaffId.ts  the ONE low-level "user → own staff id" lookup ({ activeOnly }); the single query behind canEditStaff, canViewCompensation, canEditTimesheet, the feedback gate, getReviewNoteAccess, loadStaffProfileDrawer, getStaffPto, and getCurrentStaffId. WHICH VARIANT IS AN ACCESS-CONTROL DECISION: relationship/eligibility checks (canGiveFeedback, getReviewNoteAccess, loadStaffProfileDrawer) pass activeOnly because they use the caller's identity to reach OTHER people's data and a terminated person's session outlives their employment; ownership checks (canEditStaff & co.) don't, since they only compare against the caller's own row — see [domains/permissions.md](./domains/permissions.md) → Resolving the caller
    staff/getCurrentStaffAccess.ts  user→staff resolver returning StaffAccessStatus (ok|incomplete|not_setup) + first-login email auto-link; called by the (app) layout + /profile-setup during SSR — was src/lib/staff.ts getCurrentStaff, folded into the actions layer (ADR 0010 straggler resolved)
    staff/getStaffEmploymentForEdit.ts  latest employment row per staff for the bulk-edit table (server-only)
    staff/getProfileCompleteness.ts  server-only cross-person read backing /people/profile-completeness: one row per staff member of PRESENCE/COUNTS/DATES only — résumé + client-intro presence and the link count are computed IN SQL so the free text NEVER reaches the client (minimise in the PROJECTION, never the JSX), skills via jsonb_array_length, both surveys counted from ONE responses pass using the shared isResponseAnswered. Three queries: staff + the whole staff_employment table reduced latest-per-staff (latestEmploymentFirst + firstPerKey, the getStaffDirectory shape) + responses. Carries its OWN requirePermission(PROFILE_COMPLETENESS_ACCESS) — an action has no layout above it
    staff/getCompensationSummaryData.ts  ANONYMIZED latest-employment comp rows (no id/name/email) for ACTIVE staff, for /dashboards/compensation; requires staff.viewCompensation; exports performanceFilterOptions (the filter enum lists BOTH dashboards use) (server-only)
    staff/getExchangeRates.ts  server-only FX read: USD-based rates from frankfurter.dev (Next fetch 12h cache), AED pegged + USD spliced in, never throws (stale fallback) — the repo's first external HTTP call ([ADR 0029](./decisions/0029-external-fx-rates-and-currency-normalization.md))
    staff/updateStaffLinks.ts staff/updateStaffClientIntro.ts staff/updateStaffResume.ts staff/updateStaffSkills.ts  edit-by-staffId mutations (+ .schema.ts); use metadata({ authorize: authorizeStaffEdit }) — authz runs in the client, not the body
    staff/updateStaffAllocationNotes.ts  (+ .schema.ts) the planner's inline, debounced allocation-notes editor — gated by the STATIC staff.edit capability (NOT authorizeStaffEdit: these are cross-person staffing notes on a management view, so own-row editing isn't the intent; the field is also read-gated on staff.edit in getAllocationsGrid so it never reaches an unprivileged client)
    staff/staffProfileMutation.ts  shared staff-mutation helpers (not an action): revalidateStaffProfile (revalidates /profile + /staff/[id], + extra paths) + assertStaffUpdated (the bespoke asserts-updated guard staff keeps instead of the generic assertRowExists)
    staff/parseResumePdf.ts  (+ .schema.ts) PDF→text extraction (unpdf) for the resume dialog; DOES NOT touch the DB — two-step parse-then-store (ADR 0013)
    staff/loadStaffProfileDrawer.ts  client-triggered INTERACTIVE read for StaffProfileDrawer (load* not get*, the ADR 0010 exception like loadOpportunityDetail). No capability gate on the ACTION (matches /staff/[id]) — instead FOUR gated slices, each null when not permitted so the drawer renders fewer sections: compensation (canViewCompensation, split out of the money-free employment facets), pto (getStaffPto self-gates on pto.review), feedback (getFeedbackAboutStaff), reviewNotes (the reporting line). null means NOT PERMITTED, never "none on file" (CompensationSection renders that itself). history is fetched SEQUENTIALLY after the Promise.all because the comp gate decides whether comp rides its employment entries. Requires an ACTIVE LINKED STAFF ROW (ownStaffId activeOnly) — Google sign-in is not domain-restricted, a terminated session outlives employment, and an action has no (app) layout above it. The payload spells out its own shape rather than passing StaffProfile through: minimise in the PROJECTION, never the JSX. The worked example in [domains/permissions.md](./domains/permissions.md)
    staff/canEditStaff.ts  staff-edit authz (ADR 0014): canEditStaff(user, staffId) → boolean (UI affordance) + authorizeStaffEdit (ActionAuthorize hook reading clientInput.staffId; own → always, other → staff.edit)
    <domain>/<verb><Thing>.ts  mutations → next-safe-action, one per file (+ .schema.ts)
    admin/                   {preview,commit}StaffImport + {preview,commit}PtoImport + commitBulkEditEmployment (publicActionClient + assertLocalhost); getUsers + commitUserChanges (manage-users: secureActionClient role:admin + assertLocalhost, mutates via Better Auth admin API); promoteSelfToAdmin (secureActionClient + assertLocalhost — the ONE deliberate direct-column role write, first-admin bootstrap escape hatch)
    crm/                     get{Companies,Contacts}Page (both take a filters arg — name search + status/location — applied to the count AND the rows) + getOpportunitiesBoard (the kanban read) + getOpportunitiesPage (the ?view=list read) + getTasks (getTasksForParent + openTasksByParent, the tasks reads) + get{Company,Contact}Detail + getOpportunity (server-only reads) + loadOpportunityDetail (client-triggered 'use server' read — load* not get*, since get* is reserved for server-only reads) + search{Companies,Contacts,Staff} (type-ahead) + {create,update}{Company,Contact} + update{Company,Contact}Field (+ .schema.ts — the inline detail-page field edits: a discriminated union on `field` (owner/location, plus relationshipStrength and isActive for contact) mirroring updateOpportunityField, so only the changed column is written; crm.edit-gated — these REPLACED the five deleted single-column actions update{Company,Contact}Owner / update{Company,Contact}Location / updateContactRelationshipStrength) + {create,update,delete}ContactRelationship (+ contactRelationship.schema.ts, a discriminated union on `kind`, and contactRelationshipChecks.ts — the cross-row same-company/different-company/no-cycle rules + mapContactRelationshipConflict; cardinality is owned by four named PARTIAL unique indexes, not the app — ADR 0052) + createOpportunity + opportunityHasProject (server-only guard backing the one-project-per-opportunity rule) + updateOpportunityField (the drawer's write path: field-scoped edits, a discriminated union on `field`, so only the changed slice is written — no stale-write clobber) + updateOpportunityPosition — all writes gated crm.edit (+.schema.ts). (The old full-record updateOpportunity + .schema.ts have been DELETED — updateOpportunityField is the sole drawer write path; writeOpportunityLinks stays live via createOpportunity.) Shared non-action helpers: revalidate.ts (revalidateCompany/revalidateContact — list+detail path, mirroring staff's revalidateStaffProfile — plus the two-sided revalidateCompanyContactRelationship and revalidateContactRelationship, since either endpoint's page can write the row) + entryMutations.ts (addEntry/updateEntryBody/deleteEntry core the nine entry-CRUD wrappers delegate to; reads via selectEntries in entryViews.ts) + opportunityLinks.ts (replaceOpportunity{Contacts,Owners,SourceStaff,SourceContacts} + writeOpportunityLinks — the four people-junctions) + assertOpportunityTransitionAllowed.ts (the single ADR 0024 delivery-transition enforcement, shared by updateOpportunityPosition + updateOpportunityField) + contactChecks.ts (assertValidManager/mapContactEmailConflict, shared by create+update contact). Source/status enum tuples + labels live in src/lib/crm/opportunity.ts, imported directly by the schema + pgEnum + UI (back-compat re-export shims removed)
    projects/                getProjectsList (server-only open read backing /projects: getProjectsInBuckets [full status sections] + getProjectsPage [offset/limit page] + getDeliveryManagerOptions [the dm-filter option set]; derives status/LoB + a role date range, filters by project/company name + line of business + delivery-manager id; REPLACED the deleted getProjectsPage.ts/ProjectRow) + searchStaff/searchCompanies (type-ahead, projects.edit-gated) + createProject — writes gated projects.edit (+.schema.ts, pure). PLUS the budgets/margin slice ([ADR 0053](./decisions/0053-project-budgets-and-margin.md)): projectBudget.schema.ts (a client-importable zod DISCRIMINATED UNION on billingType, mirroring the projects_budget_shape CHECK; shared by all three write paths — the T&M arm takes NO input, since the rate card is in code) + projectBudgetWrite.ts (just projectBudgetColumns — explicit nulls on the T&M branch so a switch away from a fixed fee clears the total; there are no rate rows to reconcile) + updateProjectBudget (+ .schema; gated projects.edit, SEPARATE from updateProject so a rename never re-submits the price; ONE `UPDATE ... RETURNING`, no transaction, the returned-nothing case IS the not-found error) + getProjectCostBasis — !! THE ONE PLACE THE projects.viewMargin DECISION IS MADE, and it lives in the READ: returns null (the absence IS the signal) before touching staff_employment, because getProjectPlan SSRs into a client component and loadOpportunityPlan is only crm.edit-gated, so sales reaches a plan. Never filter cost in the UI. Both plan readers now also return costBasis + exchangeRates + PlanProject.budget (the three columns only — no rate card, the client imports it). PLUS getProjectsMarginContext — a React cache()-wrapped, REQUEST-SCOPED {rates, costBasis, nativeCurrencies} for the LIST (the grouped view fires five list reads in parallel and getRoleTypeAverageCostsUsd scans all of staff_employment, so the promise is memoized once per request); cost still comes ONLY from getProjectCostBasis, so the viewMargin decision stays in one place. ProjectListItem consequently carries billingType + flags[] + latestHealth/latestHealthDate (the newest delivery note, UNGATED) + margin precomputed for BOTH display currencies (null ⇒ no viewMargin) — deliberately NOT native amounts + rates like the detail page, so no per-role compensation cost ships to a list ([ADR 0057](./decisions/0057-projects-list-margin-and-derived-flags.md)). PLUS the delivery-notes slice ([ADR 0059](./decisions/0059-project-delivery-notes-and-list-health.md)): getProjectDeliveryNotes (server-only OPEN read — no user, no mask, returns a bare array; also exports latestDeliveryNoteFirst, the desc(noteDate),desc(createdAt) order clause SHARED with getProjectsList's distinct-on so list health and the detail log can't disagree) + deliveryNotes.schema.ts (pure; one deliveryNoteFields spread into create+update) + createProjectDeliveryNote / updateProjectDeliveryNote / deleteProjectDeliveryNote — ALL THREE the plain static projects.edit gate, deliberately NOT author-only (a shared operational record; authorStaffId is ATTRIBUTION ONLY and must never become an authorization input) — the inverse of self-evaluations' author-only rule. Derived-status bucketing done in SQL by lib/projects/project-status-sql.ts (derivedStatusCondition over four buckets tentative/paused/active/other[cancelled-only] — correlated EXISTS, LOCKSTEP with the JS mirror statusesMatchBucket + guarded by project-derived.test.ts)
    feedback/                createFeedback (+ .schema) + authorizeFeedback (canGiveFeedback + authorizeFeedbackCreate hook — giving open to active staff, no capability) + getFeedbackAboutMe/getFeedbackIGave/getFeedbackDetail/getFeedbackAboutReports/getFeedbackAboutStaff (server-only reads, privacy enforced by projection; getFeedbackAboutReports = the "Your reports" tab — same feedback.review gate as getFeedbackDetail, scoped by staff.managerId, null = hide the tab, [ADR 0047](./decisions/0047-feedback-reports-scoping-not-granting.md); getFeedbackAboutStaff = the per-person profile/drawer tab, a two-tier tagged union whose SELF branch is checked FIRST so your own profile yields the limited recipient tier even holding feedback.review, [ADR 0050](./decisions/0050-profile-peer-feedback-tab.md)) + searchStaffForFeedback (recipient picker) — see [domains/performance.md](./domains/performance.md)
    performance/             getRatingsSummaryData (ANONYMIZED level rows for active staff, ratings.view — NOTE the rows still carry comp AMOUNTS (RatingRecord.employment = full CompensationDimensions), read by BOTH /dashboards/levels (dimensions only, renders no money) and conditionally by /dashboards/compensation (the by-level table); both pages get their filter option lists from staff/getCompensationSummaryData's performanceFilterOptions — there is no separate ratingsFilterOptions) + getStaffRatingsForEdit (one row per active staff for the edit table, ratings.view) — both server-only reads — + saveStaffEvaluation (+ .schema, ratings.edit: one new dated staff_rating row per genuinely-changed staff, drops no-ops, rejects date < latest; revalidates /dashboards/levels + its edit page) — the staff-levels slice ([ADR 0032](./decisions/0032-staff-rating-levels-effective-dated-manager-only.md), UI split by [ADR 0044](./decisions/0044-performance-dashboards-split-by-permission.md)); PLUS the compensation-change-plan slice: getCompensationPlans / getCompensationPlan / getStaffForCompensationPlan (server-only reads) + SIX mutations: createCompensationPlan / updateCompensationPlan (rename + effective date, draft only — the editor's Edit dialog) / deleteCompensationPlan (the plans-list confirm) / setCompensationPlanStaff (takes the COMPLETE desired membership {planId, staffIds}, diffs it server-side, inserts + deletes in ONE transaction; replaced the deleted add/removeCompensationPlanStaff pair) / saveCompensationPlanItem (the per-field autosave endpoint; no revalidatePath, by design) / commitCompensationPlan (writes staff_rating rows + freezes a comp snapshot — NEVER staff_employment) + compensationPlanWrites.ts (requireDraftPlan / buildPlanItems / planPaths), all gated by the composite COMPENSATION_PLAN_ACCESS ([ADR 0046](./decisions/0046-compensation-change-plans-rating-writing-proposals.md), editor round [ADR 0051](./decisions/0051-plan-editor-status-ladder-display-units-and-level-targets.md)); PLUS the review-notes slice: reviewNoteAccess.ts — !! THE ONE RELATIONSHIP-BASED GATE IN THE CODEBASE !! (getReviewNoteAccess + authorizeReviewNoteCreate/authorizeReviewNoteMutate: staff.managerId decides access, role capabilities are NOT consulted, self path returns before managerId is read, the mutate hook adds the author path — applied AFTER the access read, as callerStaffId !== null && note.authorUserId === user.id, so it survives a team change but NOT a departure — and denies a missing note identically to a forbidden one. Apart from admin every path requires an ACTIVE linked staff row) + getStaffReviewNotes (server-only read, three projections, two null exits — the early one refuses an inactive/unlinked non-admin caller before the query, matching the hook) + createReviewNote / updateReviewNote / shareReviewNote (one-way, no un-share) / deleteReviewNote (+ one family reviewNotes.schema.ts — status is absent from every input schema), [ADR 0049](./decisions/0049-review-notes-reporting-line-as-authorization-boundary.md); PLUS getStaffEvaluationHistory (server-only read: ONE person's FULL dated staff_rating history, newest first — level + subratings + who saved it — where getRatingsSummaryData/getStaffRatingsForEdit both read only the LATEST row across everyone. Gated on ratings.view with NO owner path, the strictest read in the app: it returns null, NOT [], for anyone without the capability, so the drawer's Evaluations tab disappears rather than showing an empty one. Feeds only loadStaffProfileDrawer today)
    timesheets/              getTimesheet + getSelectableProjects + getTimesheetPrefill (server-only reads, self-scoped / open; prefill derives allocations from project_roles + approved staff_pto, never the PTO type) + saveTimesheet/submitTimesheet/reopenTimesheet (mutations) + canEditTimesheet (canEditTimesheet(user, {staffId,weekStartDate}) → boolean + authorizeTimesheetEdit hook: own-in-window always, else timesheets.edit) (+ saveTimesheet.schema.ts / timesheetWeek.schema.ts) — see [domains/timesheets.md](./domains/timesheets.md), [ADR 0027](./decisions/0027-timesheet-weekly-model-and-edit-window.md)
    responses/               getManualOfMe + getWaysOfWorking (server-only per-person reads; the latter's answeredCount uses the shared lib/staff/survey-answers isResponseAnswered, as does the cross-person staff/getProfileCompleteness) + upsertResponse (+ .schema.ts) — the generic (staffId, questionId) survey store backing both profile surveys ([ADR 0028](./decisions/0028-generic-responses-table-app-validated-question-ids.md))
    allocations/             getAllocationsGrid (server-only read backing /allocations: active staff + their project_roles + PTO folded into planner rows via lib/allocations/allocations-grid.ts; requires the staffing view — exports allocationsFilterOptions; allocation-notes column only populated for staff.edit holders) — see [domains/allocations.md](./domains/allocations.md)
    utilization/             getUtilizationReport(range) — server-only read backing /dashboards/utilization, a PROJECTION not a calculator (all bucketing/percentages are pure client math in lib/utilization/). Six queries: staff whose employment window overlaps the range (NOT staff.isActive — that would make departures structurally zero), the latest-employment fold (latestEmploymentFirst + firstPerKey, the getAllocationsGrid shape) filtered to isBillable, staffed tentative/confirmed project_roles overlapping the range, approved staff_pto DATES ONLY (leave TYPE deliberately never selected — it stays behind pto.review), the all-time earliest confirmed role per person (the joiner "days to first placement" metric needs a role predating the range), and TWO SCOPE-DEPENDENT timesheet queries. !! THE CONFIRMED SERIES IS GATED ON timesheets.edit !!: without it both timesheet queries carry eq(timesheets.staffId, viewerStaffId) as a REAL SQL PREDICATE (never a post-filter, never a UI hide) and confirmedStaffIds — the single signal the client reads — is [ownId] instead of null, so cohort-level confirmed figures come back null, NOT 0. The PAGE itself is ungated (the planned series only re-aggregates what getAllocationsGrid already discloses). Re-exports utilizationFilterOptions = STAFF_FILTER_OPTIONS — see [domains/utilization.md](./domains/utilization.md), [ADR 0060](./decisions/0060-utilization-report-two-series-and-timesheet-disclosure.md)
    cities/                  searchCities + nearbyCityLabels — type-ahead + "nearby"-radius lookups over the static world-cities list (lib/cities/), backing the location comboboxes/filters; secureActionClient auth-gate only, NO capability gate (static public reference data, no user/tenant data), and the dataset stays server-side (never bundles to the browser)
    shared/                  staffHourlyCost.ts — !! COMPENSATION-DERIVED, DO NOT CALL DIRECTLY FROM A READER !! (hourlyCostOf: hourlyRate as-is, or annual base ÷ HOURS_PER_YEAR via convertCompUnit — NOT scaled by utilizationTarget, bonuses excluded + getStaffHourlyCosts, the latest-employment fold + getRoleTypeAverageCostsUsd, per-discipline averages computed in USD so no individual amount leaves the server and the client's currency toggle needs no re-read; SPECIALIST pools all billable roles, a role type with no staff is ABSENT not 0). Route through projects/getProjectCostBasis, which owns the projects.viewMargin gate ([ADR 0053](./decisions/0053-project-budgets-and-margin.md)); entitySearch.ts — shared searchStaffByName/searchCompaniesByName query bodies (server-only) reused by BOTH crm/ and projects/ search actions (each wraps with its own permission gate); contactName.ts — contactNameSql, the SQL-side "First Last" expression (counterpart to lib/contact-name.ts), shared by getCompanyDetail/getOpportunity/getContactsPage/getContactDetail; assertRowExists.ts — generic asserts-non-empty guard for the "empty result means the row is gone" pattern (from a .returning() or .limit(1)), throwing a user-safe "That <entity> no longer exists." — adopted across CRM + projects actions in place of ~30 hand-rolled if-length-0-throw sites (staff keeps its bespoke assertStaffUpdated)
  components/                React components; ui/ = vendored shadcn primitives,
                             form/ = shared form-dialog + form-field (with a labelAction slot) + enum-select (EnumSelect) + filters (promoted shared filter controls: FilterLabel/SelectFilter/SearchableSelectFilter/MultiSelectFilter/SegmentedFilter/TriStateFilter + SearchableOption + ALL/TRISTATE sentinels, used by the admin + directory + CRM + allocations + projects lists; SearchableSelectFilter = clearable searchable single-select over {id,name} for long option sets like staff, e.g. the projects delivery-manager filter; MultiSelectFilter is a chips-Combobox multiselect with NO ALL sentinel — empty = matches nothing) + search-filter (useUrlSearchFilter + SearchFilter — the debounced URL-backed `q` search shared by all four list filter bars: companies/contacts/opportunities/projects; extracted from four verbatim copies) + the entity pickers entity-combobox (EntityCombobox, single-select) + entity-multi-combobox (EntityMultiCombobox) + endpoint-picker (EndpointPicker — one end of a bounded date range as a compact button + calendar popover, "YYYY-MM-DD" strings in/out, no clear affordance; EXTRACTED from allocations/planner-range.tsx so the planner and the utilization report share one control) + inline-edit-field (InlineEditField — the click-to-edit-in-place control behind the opportunity drawer's per-field edits) + form helpers apply-server-issues + combobox-empty-message (searchEmptyMessage) + stop-bubbling-submit,
                             app-shell/ + auth/ + brand/ = the UI shell, admin/ = shared csv-import + editable-table + table-filters (still OWNS SortHeader; re-export shim for the rest) + data-table (a RE-EXPORT SHIM — the real DataTable moved to top-level components/data-table.tsx once a non-admin caller appeared; optional defaultSorting turns on sorting, omit it to keep server row order) + staff-import + pto-import + bulk-edit-roles + manage-users + promote-self-button UI,
                             staff/ = shared ProfileView (backs /profile + /staff/[id], two-column tabbed layout per #33; the tab set is VIEWER-DEPENDENT — 5-8 tabs, since the Peer feedback + Review notes + Self-evaluations tabs render only when their read is non-null) + staff-profile-drawer (the read-only Sheet-based review pane opened from a compensation-plan row; the app's SECOND Sheet consumer, mirroring the CRM opportunity detail sheet; 3-9 tabs — Overview / Projects / [Time off] / [Peer feedback] / [Review notes] / [Self-evaluations] / [Evaluations] / [Bonuses] / History, the bracketed six rendered only when their gated read came back non-null; Self-evaluations = their own words, Evaluations = the level a manager assigned, NEVER shown against each other — and reuses the profile's own CompensationSection/SkillsSection/StaffProjectsSection/PtoContent/HistoryTimeline/SelfEvaluationPanel so the two can't drift) + directory/cards + edit dialogs (links, client-intro, resume) + the profile sections skills-section/compensation-section/pto-section (+ pto-list)/staff-projects-section/manual-of-me-section + edit-skills-form + manual-of-me-guide + history-timeline (the person's employment history, formerly the "history sheet") + profile-completeness-table (the /people/profile-completeness read-only sortable DataTable — in-memory filters + sorting, nothing in the URL),
                             crm/ = add-{company,contact,opportunity}-dialog + {company,contact}-fields (shared field groups) + opportunity-form-fields (OpportunityFields + opportunityValuesToInput) + company-combobox + create-{company,contact}-inline-dialog + {companies,contacts,opportunities}-table + opportunity-display (STATUS_SELECT_LABELS only) (the entity-combobox/entity-multi-combobox pickers moved to form/; pagination-controls promoted to top-level shared),
                             projects/ = add-project-dialog (minimal: name + company + budget) + project-card + projects-grid (ProjectsGrid + ProjectsSection) + projects-list-filters (URL-backed search + LoB + delivery-manager filters) + project-status-badge + role-fields (the role form fragment shared by both role dialogs) + budget-fields (the budget fragment shared by ALL THREE budget forms; both modes' fields always mounted, inactive half dropped at submit) + budget-dialog (set/edit a budget → updateProjectBudget) + budget-summary-panel (revenue/cost/margin + CAD-USD toggle; mounted on BOTH plan surfaces; cost+margin render only when the server sent a cost basis) + plan-summary-tiles (the tile row both plan surfaces duplicated; optional deliveryManagers + health tiles, each omitted on the surface that has nothing to show) + use-project-margin (client-side compute + display currency, so panel and grid can't disagree) + detail/ (the delivery-side project editor — project-detail-view [FOUR tabs: Timeline · Roles · Delivery notes · Time off] + project-name-field + delivery-managers-field + use-project-inline-save + project-role-dialog + delivery-notes-panel [the dated health log + inline composer/editor + ConfirmDialog delete; StarRating max=10 with a hover-preview label, safe here because nothing wraps it in a link — the LIST card renders health as text for exactly that reason]; [ADR 0045](./decisions/0045-project-page-as-delivery-side-role-editor.md)) + opportunity-plan/ (the weekly Gantt-like opportunity planner — opportunity-project-plan entry + planner-grid [optional margins prop → a third line in the sticky label cell, NOT a new column: left-56 is hand-twinned to PLANNER_LABEL_COL and shared with the allocations grid] + create-project-dialog [the budget-only create-from-opportunity form, which replaced the board's ConfirmDialog] + edit-project-dialog + role-dialog + extend-dialog; mounted inside the CRM opportunity detail sheet),
                             feedback/ = feedback-form (give-feedback) + feedback-about-me + feedback-given-table + feedback-about-reports-table (reviewer's direct-reports list, in-memory filters) + staff-feedback-panel (the per-person profile/drawer tab, presentational over the two-tier view) + feedback-detail-dialog (the full-content dialog, shared by both tables AND the panel) + feedback-detail-fields (one item's full content, shared by that dialog + the detail page),
                             performance/ = THREE dashboards, one per route ([ADR 0044](./decisions/0044-performance-dashboards-split-by-permission.md), [ADR 0055](./decisions/0055-nav-dashboards-vs-people-management.md)): compensation-dashboard (client, /dashboards/compensation — every money table: KPI cards, by-role table, comp-BY-LEVEL table (rendered only when the optional ratingRecords prop is passed, i.e. viewer holds ratings.view), distribution scatter + Compensation↔Hourly metric toggle) + levels-dashboard (client, /dashboards/levels — RENAMED from performance-dashboard by ADR 0055; avg-level/unrated cards, level-distribution bar chart, avg-level-by-role, subratings-by-category tables; NO money, so no rates/FX/currency toggle) + bonus-dashboard (client, /dashboards/bonuses — a thin shell: the filter bar + its page-local bonus-type SegmentedFilter + the write-gated "Manage payments" link) → bonus-breakdown (owns EVERY number on that page: one filtered+FX-normalized payment set, stat cards, three single-axis tables, the shared CROSS_CUT_AXES/AXES heading+label constants) → bonus-type-matrix (type × LoB/role, axis picked by a local ToggleGroup; MONEY-ONLY cells and nothing rendered below two type columns — [ADR 0056](./decisions/0056-bonus-type-cross-cut-matrix.md)) + bonus-payments-manager (client, /people/bonus-payments — the entry table + create/edit/delete dialogs) + dashboard-filters (shared control bar: useDashboardFilters (LoB/employment/role + CAD-USD) + DashboardFilterBar (optional rates → the currency toggle renders iff rates are passed, so no toggle without the FX to honour it; optional extraFilters → the slot for a dimension only ONE dashboard has, which is why bonus type is NOT in the hook) + matchesFilters (null dimensions pass only while every filter is "All") + FilterOptions type — each dashboard instantiates its OWN state, no shared bar) + stat-card (reusable KPI tile, extracted from the Home page) + compensation-scatter (hand-rolled inline-SVG single-series scatter, no charting lib) + level-distribution-bar-chart (hand-rolled SVG bar chart, zero baseline + hover tooltip) + edit-levels (EditableTable batch level editor, name + role + line-of-business filters) + compensation-plans/ (the save-on-edit plan editor: plans-list [+ delete confirm] + new-plan-dialog + edit-plan-dialog [rename + effective date, draft only] + manage-plan-staff [the membership roster on its OWN /staff page, submits the whole checked set at once — there is NO add-staff-dialog and no per-row remove] + plan-editor + plan-row + plan-expanded-panel + planned-comp-field + commit-plan-dialog + plan-toolbar + plan-columns + plan-format + plan-view + plan-row-view + use-plan-autosave — built on the raw Table primitives, NOT EditableTable; the app's first expandable rows; a row's name opens the shared StaffProfileDrawer) + review-notes-panel (the Review notes tab — DISCRETE saves + toasts, deliberately NOT the autosave queue; optional onChanged so a client-fetched host re-loads itself instead of router.refresh()) + self-evaluation-panel/-record/-form (the Self-evaluations tab, same onChanged convention, one explicit submit and no autosave; the drawer passes readOnly as a DISPLAY constraint, not a gate. NOTE: self-evaluation-record MUST NOT import SELF_EVALUATION_QUESTIONS — every prompt is rendered off the record's own snapshot, so only the form and the write path read the current question set) + evaluation-history (the Evaluations tab: purely presentational over getStaffEvaluationHistory, one dated entry per staff_rating row — level Badge + subrating labels from the FLATTENED RUBRIC_LABELS rather than the person's current role, since a stored key can predate a role change and a raw key beats dropping the score; same left-border timeline as history-timeline so the two history surfaces read alike, and it takes the non-null array — the ratings.view gate is the CALLER's job),
                             timesheets/ = timesheet-week (client weekly grid: searchable add-project-dialog + "Fill in PTO"/"Fill in allocations" prefill buttons, per-day totals + cap warning, Save/Submit/Reopen) + add-project-dialog (searchable target picker: allocated-first, all projects, non-billable buckets),
                             allocations/ = allocations-planner (client: search + staff filters, day/week/month range toggle) + allocations-grid (AllocationsGrid + AllocationsLegend, the read-only planner grid) + planner-range (granularity/range picker — now wraps the SHARED form/endpoint-picker) + allocation-note-cell (the debounced inline manager-only note editor),
                             utilization/ = utilization-report (the client shell owning the two IN-MEMORY filters — LoB + the forecast toggle — over the once-fetched projection; only the date range round-trips, because it bounds the query) + seven cards (utilization/headcount/roles/bench/pto/staff-breakdown/lob-alignment) + report-primitives (ReportSection + CoverageNote — the standing submitted-week caveat on every confirmed number) + utilization-filters,
                             planner/ = planner-columns.ts (shared literal Tailwind column widths so the allocations grid, opportunity project plan, and project detail view line up under table-fixed),
                             plus top-level shared cells empty-cell.tsx + external-link.tsx + internal-link.tsx (styled Next Link) + icon-button.tsx (icon-only button + tooltip) + contact-link.tsx (MailLink/PhoneLink) + pagination-controls.tsx (Link-based prev/next, reused by CRM + projects lists) + confirm-dialog.tsx (shared AlertDialog confirm wrapper) + fx-rate-note.tsx (FxRateNote({rates, from, to}) — the conversion caveat stated ONCE per panel beside the currency selector, NAMING the rates: one currency inline ("1 USD = 1.37 CAD"), several summarised with the pairs in the tooltip, always closing with the asOf date or the stale-fallback sentence; renders null when nothing was converted. Fed by ProjectMargin.convertedFrom. NOTE a per-value variant [converted-money.tsx] was built and deleted — don't reintroduce it) + inline-notice.tsx (the hairline non-blocking notice strip — extracted from the two open-coded timesheet banners; never sets aria-invalid, never blocks a submit) + empty-state.tsx (the "No … yet" list placeholder, optional bordered variant, shared across CRM/projects/feedback tables + detail sections) + table-density.ts (ROOMY_TABLE — the shared cell-padding override class string for the vendored Table's too-tight p-2; used by contacts-table + the shared DetailTable, see ui.md)
  hooks/                     useDebouncedValue (debounce, used by company-combobox), useMobile, useSignOut (shared error-aware sign-out flow behind the sidebar footer + SignOutButton), use-autosave-queue (the generic save-on-edit engine: dirty-set + per-key debounce + single-flight drain; consumers supply save(key) — used by the profile surveys and the compensation-plan editor, see [ui.md](./ui.md))
  lib/                       organized into domain subfolders (below); every file kept its name — only the import prefix changed (@/lib/<file> → @/lib/<group>/<file>). db/ was NOT moved. ([ADR 0036](./decisions/0036-lib-organized-by-domain-subfolders.md))
    core/                    cross-cutting plumbing:
      action.ts              publicActionClient + secureActionClient (the core); metadata-driven gates: role + permission + authorize (ActionAuthorize hook), all enforced before the body
      errors.ts              UserSafeActionError (user-safe error channel)
      logger.ts              structured logging used by the action layer
      utils.ts               cn()
      constants.ts           APP_NAME / APP_DESCRIPTION (shared app strings)
      like.ts                escapeLike() — escape LIKE/ILIKE metacharacters so user input matches literally
      pagination.ts          shared server-side pagination primitives (page size, parsePage, clampPage, {rows,total,page,pageSize,pageCount} envelope) — used by all CRM list reads
      search.ts              shared type-ahead search primitives (SEARCH_LIMIT, searchQuerySchema, the SearchAction contract type the entity pickers accept) — used by search{Companies,Contacts,Staff}
      collections.ts         firstPerKey — fold pre-sorted rows into a Map keeping the FIRST per key (staff picks latest-employment-per-staff); groupPerKey — the sibling keeping ALL rows per key, for "the latest row AND the one before it" (bound the input first; used to derive each person's previous comp change in the plan editor read)
      list-href.ts           shared querystring helpers for the URL-backed list views — the four filter bars (companies/contacts/opportunities/projects), the shared useUrlSearchFilter hook, and PaginationControls all rebuild the current querystring differing only by base path + page-key, so hrefWith/buildHref lives here once
    auth/                    auth + access control:
      auth.ts auth-client.ts better-auth server + client (admin plugin wired with ac/roles); getCurrentUser/checkAuth
      permissions.ts         RBAC single source of truth: statement, roles, matrix, userHasPermission/requirePermission (permissions.test.ts asserts the matrix — ADR 0037 removed the broad unit suite but kept this RBAC-matrix test; a handful of small pure-logic tests have since been added back case by case, each guarding a cross-representation invariant the types can't express — currently allocations-grid, timesheet-grid, timesheet-week, project-derived, project-planner-grid, rating-rubric, rating-stats, compensation-plan, org-chart, utilization-report, utilization-range — so `bun run check` runs those `*.test.ts` files too; [ADR 0037](./decisions/0037-unit-tests-removed-except-rbac-matrix.md))
      admin.ts               isLocalhost()/assertLocalhost() — the admin-area security boundary (host header)
    schemas/                 shared zod field primitives:
      id-schema.ts           shared entity-id zod fields: id (non-empty), optionalId, idList (defaults to [])
      date-schema.ts         shared calendar-date zod field: ISO_DATE regex + isCalendarDate (round-trip validity, reused by import/csv-import/parse.ts) + dateString ("YYYY-MM-DD"), the date counterpart to id/idList
      url-schema.ts          shared optional-URL zod field (blank→null; bare host → https:// normalised)
      text-schema.ts         shared optional free-text zod field (blank/whitespace→null, else trimmed)
    format/                  display + money:
      format.ts              humanizeEnum() + formatDate() + formatTimestamp() + formatShortDate() (compact "Jul 18, 2026" for cells/logs) + initialsFor() + parseIsoDate()/formatIsoDate() — display helpers + the shared drift-safe "YYYY-MM-DD" ↔ Date parse/format (wall-clock, no UTC drift)
      currency.ts            CURRENCY tuple + formatMoney (optional Intl.NumberFormatOptions 3rd arg — used for whole-dollar aggregates) + aggregateMoneyFormatters(currency) → { money, range } (whole-dollar formatter pair, em dash for null — extracted from the merged dashboard's two inline copies; now used only by the Compensation dashboard, which owns every money table) + normalizeCurrency (pure) — single source for the currency pgEnum, comp import schema, and display ([ADR 0020](./decisions/0020-compensation-effective-dated-import-only.md))
      fx.ts                  AED_PER_USD + FALLBACK_USD_RATES + convert(amount, from, to, usdRates) — pure USD-cross-rate FX conversion, client-importable. That is the WHOLE surface: there is deliberately no per-value provenance type (an FxAmount/convertTagged/sumFx trio was added with the project budget panel and then removed — the caveat is a per-PANEL fact, tracked as ProjectMargin.convertedFrom and rendered by components/fx-rate-note.tsx). Consumed by /dashboards/compensation AND the project budget panel ([ADR 0029](./decisions/0029-external-fx-rates-and-currency-normalization.md), [ADR 0053](./decisions/0053-project-budgets-and-margin.md))
    crm/                     CRM enum tuples + derived helpers:
      line-of-business.ts    shared LINE_OF_BUSINESS tuple + labels (pure, client-importable) — single source for the lineOfBusinessEnum pgEnum + the CRM/projects zod schemas + forms (carried by staff_employment, opportunities, project_roles — moved back onto the role, [ADR 0033](./decisions/0033-line-of-business-on-role-derived-project-status.md))
      opportunity-pipeline.ts OPPORTUNITY_GROUPS + status-group derivation + fractional-ordering helpers (pure) — the kanban's two-level grouping lives in code, not the DB ([ADR 0021](./decisions/0021-opportunity-pipeline-groups-and-fractional-ordering.md))
      contact-name.ts        contactName({firstName,lastName}) — the one place the "First Last" display convention lives
      contact-status.ts      ACTIVE_LABEL/INACTIVE_LABEL + contactStatusLabel + the two INACTIVE_*_EXPLANATION strings (pure, client-importable) — the display side of contacts.isActive, imported by all four "Inactive" badge sites so the word can't drift; "Inactive" not "former" on purpose (see domains/crm.md#contact-status-active--inactive)
      (also opportunity.ts, company-status.ts, relationship-strength.ts, contact-relationship.ts, company-contact-relationship.ts)
    cities/                  location reference data (world-cities), NOT a DB table:
      cities.ts              the static world-cities dataset + pure lookups over it (searchCities, citiesNear) — location is stored as a free-text "City, CC" label chosen from this list, not an FK (see data-model.md); parsed once at module load
      world-cities.json      the column-oriented dataset (parallel arrays); a small US/CA sample for dev, swap the full set in at the same path
    projects/                project enum tuples + derived status/LoB + the money math:
      project-role-type.ts   PROJECT_ROLE_TYPE tuple + labels (pure, client-importable) — single source for the projectRoleTypeEnum pgEnum, the project-role zod schema, and forms (ENGINEER/DESIGNER/ARCHITECT/QA/SPECIALIST) + STAFF_ROLE_FOR_PROJECT_ROLE_TYPE (the staff_employment.role each maps to for open-role costing; SPECIALIST → null ⇒ average all billable disciplines). NOTE the `import type { Role }` is deliberate — a value import would close a runtime cycle via staff-schema
      project-billing.ts     BILLING_TYPES tuple + labels (pure) — single source for the project_billing_type pgEnum, the budget zod discriminated union, and the dialogs ([ADR 0053](./decisions/0053-project-budgets-and-margin.md))
      bill-rates.ts          BILL_RATES (TOTAL over role types) + BILL_RATE_CURRENCY + BILL_RATES_REVIEWED_ON + standardRateCard() (display rows) + isFlatRateCard() — THE company-wide rate card as CODE-AS-POLICY, not a table and not per project (the compensation-targets.ts precedent; a project_role_rates table was built and dropped, [ADR 0053](./decisions/0053-project-budgets-and-margin.md) §1–2). Total map ⇒ "a role type with no bill rate" is unrepresentable. ⚠️ the shipped figures are a flat 225 USD PLACEHOLDER
      project-margin.ts      pure revenue/cost/margin over a plan's roles (+ .test.ts, 22 tests): roleBillableHours (real Mon–Fri weekdays × hoursPerDay — NEVER the grid's bucket percentages, [ADR 0040](./decisions/0040-allocations-planner-granularity.md)) + countsTowardBudget (everything but cancelled; PTO deliberately not netted) + computeProjectMargin (takes NO rate card — imports BILL_RATES; fixed-fee revenue is PROJECT-LEVEL ONLY, per-role null; marginPercent null when revenue is 0; every cost carries a RoleCostBasis; amounts are plain numbers already in the display currency, with convertedFrom listing the currencies a rate was actually applied to — a per-PANEL caveat, see fx-rate-note.tsx) + resolveDisplayCurrency({budgetCurrency}) + marginTone (1-decimal, for a percentage) + marginAmountTone (whole-dollar, for the headline money — a −$0.30 margin renders "CA$0" and mustn't be red). NO mixedRateCurrencies (deleted with the per-project rate card). Client-importable, so the server read and the client currency toggle share one implementation ([ADR 0053](./decisions/0053-project-budgets-and-margin.md)) — BUT note the /projects list converts server-side instead, precomputing both display currencies ([ADR 0057](./decisions/0057-projects-list-margin-and-derived-flags.md))
      project-flags.ts       the /projects list's DERIVED RISK TAGS + their thresholds as CODE-AS-POLICY (+ .test.ts, 28 tests): PROJECT_FLAGS (negativeMargin → lowHealth → lowMargin → endingSoon, worst-first) + labels + badge variants (only the loss is destructive) + projectFlags(input) over a predicate table — the company-status.ts / project-derived.ts shape, so the read and the card share one definition. ENDING_SOON_DAYS 14 · NEGATIVE_MARGIN_AT_OR_BELOW 0 (suppresses lowMargin) · LOW_MARGIN_PERCENT .25 OR LOW_MARGIN_AMOUNT 10k · MARGIN_FLAG_CURRENCY CAD (flags are ALWAYS judged in CAD server-side, never recomputed on the client, so a tag can't follow the display toggle) · LOW_PROJECT_HEALTH_AT_OR_BELOW 4 (inclusive — the POLICY half of the health scale, which is why it doesn't live in project-health.ts) · PROJECT_FLAGS_REVIEWED_ON. Cancelled projects, unknown/withheld margin and UNRATED health ⇒ no flags; note lowHealth is the one tag NOT capability-gated, since health isn't compensation-derived ([ADR 0057](./decisions/0057-projects-list-margin-and-derived-flags.md), [ADR 0059](./decisions/0059-project-delivery-notes-and-list-health.md))
      project-health.ts      PROJECT_HEALTH_MIN/MAX (1–10) + ten distinct labels (Critical→Exemplary) + projectHealthLabel + PROJECT_HEALTH_UNRATED_LABEL "Not rated" (+ .test.ts, 5 tests) — the delivery-note health scale as ONE source for the check constraint's bounds, the zod schema, the StarRating input, the list card and the detail tile. Vocabulary only: what counts as LOW is policy and lives in project-flags.ts ([ADR 0059](./decisions/0059-project-delivery-notes-and-list-health.md))
      delivery-note.ts       DELIVERY_NOTE_TITLE_MAX/BODY_MAX + the shared hint/placeholder strings (pure) — split from the scale module so project-flags.ts can import the scale without inheriting a note's text limits
      project-role-status.ts PROJECT_ROLE_STATUSES tuple + labels + badge variants + DEFAULT (pure) — single source for the projectRoleStatusEnum pgEnum (four states: tentative/confirmed/paused/cancelled), zod, the planner, and the derived ProjectStatusBadge ([ADR 0031](./decisions/0031-opportunity-project-planner-and-role-status.md), [ADR 0033](./decisions/0033-line-of-business-on-role-derived-project-status.md))
      project-derived.ts     deriveProjectStatus / deriveProjectLinesOfBusiness (pure) — a project stores no status/LoB; both are computed from its roles, shared by reads + UI (replaced the deleted project-status.ts; [ADR 0033](./decisions/0033-line-of-business-on-role-derived-project-status.md))
      plan-summary.ts        pure helpers for the project-plan summary tiles (date-range + delivery-manager labels); client-importable (no db/React) so the opportunity Project-plan tab and the standalone project detail page render identical stats from one source
      (also project-planner-grid.ts — pure planner grid math)
    staff/                   staff enums, helpers, and the staff/PTO CSV importers:
      staff-employment.ts    latestEmploymentFirst — the canonical "current employment wins" .orderBy fragment (ADR 0007 effective-dating tiebreak; pure, no db); shared by every latest-employment read (directory, profile, history, comp summary, bulk-edit, staff import)
      staff-enums.ts         label maps for the staff pgEnums — ROLE_LABELS/EMPLOYMENT_TYPE_LABELS/BILLABLE_TYPE_LABELS/PTO_TYPE_LABELS (+ union types) + the NON_BILLABLE_ROLES set & isBillableRole predicate (single source of the billable/overhead split, shared by the staff-import isBillable derivation and the allocations role-filter default), pure/client-safe; fixes humanizeEnum acronym mangling (QA→"Qa"). NOTE: the enum VALUE tuples still live in staff-schema.ts (derived via pgEnum.enumValues), deliberately NOT hoisted here — see [staff-profiles.md](./domains/staff-profiles.md)
      manual-of-me.ts        the Manual-of-Me question catalogue (ids + prompts, pure/client-safe) — source of truth for the seven free-text question ids the responses store validates against ([ADR 0028](./decisions/0028-generic-responses-table-app-validated-question-ids.md))
      skills.ts              the hardcoded skills catalogue (SKILL_CATEGORIES/ALL_SKILLS + PROFICIENCY_LEVELS, client-safe) + filter helpers — single source for the picker, the zod schema, and staff.skills ([ADR 0018](./decisions/0018-skills-inline-jsonb-catalogue.md))
      staff-rating.ts        RATING_LEVELS + MIN/MAX_RATING_LEVEL + formatLevel/formatAverageLevel + encode/decodeLevelValue (pure, client-safe) — single source for the staff_rating CHECK bounds, the eval zod schema, and level display ([ADR 0032](./decisions/0032-staff-rating-levels-effective-dated-manager-only.md))
      staff-rating-history.ts latestRatingFirst — the "current rating wins" .orderBy fragment (mirrors staff-employment.ts; server-only, keeps drizzle out of the client bundle)
      employment.ts          the billability invariants (isEmploymentInvariantSatisfied + normalizeEmploymentFacts, pure) — single source shared by staff-import, the bulk-edit schema/form
      pdf-upload.ts          shared resume-PDF size constants (MAX_PDF_BYTES + derived MAX_PDF_BASE64_CHARS + message) so the client guard (edit-resume-dialog) and server backstop (parseResumePdf.schema) can't drift
      staff-import/          CSV staff import: transform.ts (pure), plan.ts (server diff), types.ts (shared zod) — builds on import/csv-import/
      pto-import/            CSV PTO import (same shape as staff-import): transform.ts / plan.ts / types.ts
      pto-working-days.ts    pure working-day (Mon–Fri) math for PTO spans; client-importable (no db/React) so the per-staff read (getStaffPto) and the project-wide read (getProjectPto) count days identically
      org-chart.ts (+ .test.ts) the /staff?view=org reporting forest: buildOrgForest (self/orphan/cycle-safe — every input row appears exactly once, whatever staff.managerId does) + pruneOrgForest (matches PLUS their ancestor chain) + orgChartMatcher/countOrgMatches (matches, not rendered rows)/countOrgNodes/defaultCollapsedIds/collapsibleIds. Pure/client-safe. Its test file is a DELIBERATE carve-out of [ADR 0037](./decisions/0037-unit-tests-removed-except-rbac-matrix.md) ([ADR 0054](./decisions/0054-staff-org-chart-dom-tree.md))
      staff-bonus.ts         bonus payments' pure core (client-safe): BONUS_TYPES tuple + labels/descriptions (single source for the staff_bonus_type pgEnum, the entry form and the dashboard) + NON_CASH_BONUS_TYPES (GIFT = equivalent value, so totals are reward spend not cash) + BONUS_PAYMENT_READ_ACCESS / BONUS_PAYMENT_WRITE_ACCESS (the read = staff.viewCompensation, the write = the staff.edit AND staff.viewCompensation conjunction) + BONUS_YEAR_PARAM & parseBonusYear. !! BONUS_YEAR_PARAM MUST STAY OUT OF ANY "use client" MODULE — a Server Component importing it from one gets a client-reference proxy, and the searchParams lookup then silently misses forever !!
      bonus-attribution.ts   employmentAsOf(rows, date) — the employment row in force ON THE PAYMENT DATE (rows newest-first), so a bonus keeps counting under the discipline held at the time; a payment predating all history falls back to the EARLIEST row (signing bonuses) rather than being dropped. Pure/client-safe
      profile-completeness.ts PROFILE_COMPLETENESS_ACCESS (the gate constant — the EXISTING staff.edit capability, no matrix change) + PROFILE_COMPLETENESS_TOTALS (denominators DERIVED from the two survey id tuples, so "n of 30" can't drift). Pure/client-safe, mirroring compensation-plan.ts / staff-bonus.ts
      survey-answers.ts      isResponseAnswered + SurveyAnswerShape — the ONE definition of "this survey question has been answered" (non-null textResponse OR non-empty listResponse), shared by getWaysOfWorking and getProfileCompleteness; the two disagreed before it existed. Pure/client-safe
      (also staff-filters.ts, staff-summary.ts, ways-of-working.ts)
    timesheets/              timesheet enums + week/grid math:
      timesheet-category.ts  TIMESHEET_CATEGORY tuple + labels (pure) — single source for the time_entry_category pgEnum, the save zod schema, and the grid's bucket labels (PTO independent of staff_pto) ([ADR 0027](./decisions/0027-timesheet-weekly-model-and-edit-window.md))
      timesheet-status.ts    TIMESHEET_STATUSES tuple + type + labels + DEFAULT (pure, client-safe) — single source; the timesheet_status pgEnum in timesheets-schema.ts is BUILT FROM it (values unchanged, no migration)
      timesheet-week.ts      week math (pure, client-importable): getWeekStart/addWeeks/getWeekDays/currentWeekStart/weeksBetween/isWithinEditWindow — weeks keyed by ISO-Monday "YYYY-MM-DD", no date library, no UTC drift
      timesheet-grid.ts      pure grid math extracted from timesheet-week.tsx (per-day/row/week totals) — component keeps only rendering
    performance/             performance aggregation + feedback enum:
      feedback-rating.ts     FEEDBACK_RATINGS tuple + labels/descriptions (pure) — single source for the feedback_rating pgEnum, the zod schema, and the rating radio ([ADR 0023](./decisions/0023-feedback-privacy-tiers.md))
      performance-stats.ts   computeGroupStats + computeByRole (pure aggregation over normalized comp rows; empty group → null, not NaN) — the Compensation dashboard's math, reused for BOTH its by-role and its by-level table (the level label becomes the group key)
      bonus-stats.ts         computeBonusStats (takes only { recipientKey, amount }, so the 1-D and 2-D row shapes share it) + computeBonusBreakdown (one axis) + computeBonusMatrix (TWO axes: type × LoB/role — given order, wholly-empty rows/cols skipped, an empty INTERSECTION is null → em dash, out-of-order values in `overall` only) + bonusGroupsCovered (the per-dimension "rows sum to overall" assertion). Only `total` is safe to show per cell — recipients are DISTINCT counts and don't sum ([ADR 0056](./decisions/0056-bonus-type-cross-cut-matrix.md)); + bonus-stats.test.ts, an [ADR 0037](./decisions/0037-unit-tests-removed-except-rbac-matrix.md) carve-out
      rating-stats.ts        computeLevelDistribution + countUnrated + computeAverageLevel + computeAverageLevelByRole (pure level math for the Levels dashboard)
      compensation-plan.ts   COMPENSATION_PLAN_ACCESS (the composite gate constant) + the status tuple/labels feeding the pgEnum + display-currency modes + planChange (the row money math; percentage computed from NATIVE amounts, so it is FX-invariant) + currentCompAmount (base for FULL_TIME, hourlyRate for HOURLY) + monthsSince — pure, client-importable (+ compensation-plan.test.ts, a deliberate [ADR 0037](./decisions/0037-unit-tests-removed-except-rbac-matrix.md) exception pinning FX-invariance and never-NaN)
    allocations/             the Allocations planner's grid math:
      allocations-grid.ts    pure grid math (rows = active staff, columns = day/week/month time buckets): the column spine, per-column allocation % from project_roles, and folding staff + roles + PTO into one row per person — client-importable (no db/React), mirrors projects/project-planner-grid.ts + reuses the timesheet week math (allocations-grid.test.ts covers it). EXPORTS HOURS_PER_DAY (was private) so the utilization report's denominator is the same 8h day this grid calls 100% — see [domains/allocations.md](./domains/allocations.md)
    utilization/             the Utilization report's math, window and formatters (all pure/client-importable):
      utilization-report.ts  !! THE AUTHORITATIVE STATEMENT OF EVERY DEFINITION IS THIS FILE'S MODULE DOCSTRING !! buildUtilizationReport folds the read's projection into all seven cards in one pass over a per-person day-level StaffLedger (the weekday spine built ONCE and shared across the cohort). TWO SERIES, NEVER SUMMED: planned (project_roles) + confirmed (submitted time_entries) + a variance. Available hours = full-time only, 8h × employed working days (HOURS_PER_DAY from allocations-grid). PTO BEATS A ROLE on the same day (so project + PTO + bench = available) but OVER-ALLOCATION IS NOT CLAMPED (the first surface that sums a person's load across projects). confirmed/variance are null — NEVER 0 — for a viewer who may not read the underlying timesheets, all the way to the render. TENTATIVE_WEIGHT = 1 (no win-probability field exists in the schema; kept as one constant so tiers can land later) + BENCH_STREAK_THRESHOLD = 5. utilization-report.test.ts (26) — an [ADR 0037](./decisions/0037-unit-tests-removed-except-rbac-matrix.md) carve-out pinning the access gate and the definitions
      utilization-range.ts   the reporting window: parseUtilizationRange (current-month default, invalid/inverted inputs degrade rather than error) + MAX_RANGE_DAYS = 366 (the read walks day-by-day per person, so an unbounded URL span is a cheap way to make the server work) + the start/end param names. utilization-range.test.ts (8)
      utilization-format.ts  display formatters, kept apart from the math. THE EM DASH IS LOAD-BEARING: null → "—" means "may not read" or "nothing to average", never zero; a genuine zero prints "0"
    import/                  shared CSV plumbing:
      csv-import/            parse.ts (key normalise, date parse, duplicate tracking), types.ts, index.ts — consumed by staff/staff-import/ + staff/pto-import/ + the import UIs
    db/
      unique-violation.ts    isUniqueViolation(error, constraint) — true for a Postgres unique violation (23505) on a named constraint, so CRM writes translate only the violations they expect into a UserSafeActionError
      foreign-key-violation.ts isForeignKeyViolation(error, constraint?) — the FK-violation (23503) counterpart (optional constraint name); shared by the CRM write actions
      db.ts                  hot-reload-safe Drizzle singleton
      schema.ts              barrel: re-exports auth-schema + staff-schema + crm-schema + opportunities-schema + projects-schema + performance-schema + timesheets-schema + responses-schema (one import for the whole schema)
      staff-schema.ts        staff / staff_employment (ONGOING terms only — the old discretionaryBonus column was dropped) / staff_bonus_payment (a dated bonus payment; staff_bonus_type enum from lib/staff/staff-bonus.ts, amount>0 CHECK, nullable-unique ripplingId, NO lineOfBusiness/role — derived at read time from the employment row in force on paymentDate) / staff_pto + domain enums (lineOfBusinessEnum built from src/lib/crm/line-of-business.ts)
      crm-schema.ts          companies / contacts (CRM slice)
      opportunities-schema.ts opportunities + 4 junction tables + source/status pgEnums + lineOfBusiness (split out of crm-schema.ts; [ADR 0025](./decisions/0025-line-of-business-on-opportunity-and-project-not-role.md))
      projects-schema.ts     projects (name + companyId + the three budget columns behind a projects_budget_shape CHECK; NO stored status/lineOfBusiness — both derived from roles, [ADR 0033](./decisions/0033-line-of-business-on-role-derived-project-status.md)) / project_delivery_managers / project_roles (carries lineOfBusiness; first cut of Allocation) / project_delivery_notes (dated delivery write-up + a 1–10 projectHealth integer under a between-1-and-10 CHECK whose bounds come from lib/projects/project-health.ts; authorStaffId → staff set-null, attribution only; index (project_id, note_date DESC, created_at DESC) — the DESCs are load-bearing; NO projects.health column, list health is derived from the latest note, [ADR 0059](./decisions/0059-project-delivery-notes-and-list-health.md)) — FOUR tables; bill rates are deliberately NOT one of them (one code-owned card in lib/projects/bill-rates.ts; a project_role_rates table was created by 0014 and dropped by 0015 — [ADR 0053](./decisions/0053-project-budgets-and-margin.md))
      performance-schema.ts  feedback (peer feedback; from/to staff FKs, feedback_rating enum) + staff_rating (overall level L0–L4; effective-dated, nullable level, CHECK 0–4, evaluatedByUserId set-null) + compensation_plan / compensation_plan_item (compensation change plans; compensation_plan_status enum, unique (planId, staffId), commit-time comp snapshot columns — NOT effective-dated, [ADR 0046](./decisions/0046-compensation-change-plans-rating-writing-proposals.md)) + performance_review_note (a manager's dated write-up; DRAFT|SHARED, reporting-line-gated, [ADR 0049](./decisions/0049-review-notes-reporting-line-as-authorization-boundary.md)) + staff_self_evaluation (a person's own dated questionnaire; answers jsonb carrying a section/prompt SNAPSHOT, selfRating on its OWN self_evaluation_rating pgEnum built from the shared FEEDBACK_RATINGS tuple, questionSetVersion — NOT effective-dated, [ADR 0058](./decisions/0058-self-evaluations-dated-records-with-snapshotted-answers.md)) — the Performance slices
      timesheets-schema.ts   timesheets / time_entries (Timesheets slice; timesheet_status enum BUILT FROM src/lib/timesheets/timesheet-status.ts + time_entry_category enum; per-day actuals, project-XOR-category CHECK)
      responses-schema.ts    responses (generic survey store; (staffId, questionId) unique → upsert; app-validated text questionId, three nullable payload shapes; backs the Manual of Me — [ADR 0028](./decisions/0028-generic-responses-table-app-validated-question-ids.md))
      auth-schema.ts         better-auth tables (generated; in OUR migrations)
      ids.ts                 generateId(prefix)
drizzle/                     generated SQL migrations
drizzle.config.ts            casing MUST match db.ts
```

Path-scoped working rules live in `.claude/rules/{server-actions,database,forms,nextjs,ui}.md` and load when the matching files are touched. Read them before writing in those areas.

## Modified Next.js 16 — concrete deltas

This is a **modified** Next.js 16 build (see [decisions/0002](./decisions/0002-modified-nextjs.md)); your training data is likely wrong about it. Always confirm against `node_modules/next/dist/docs/` (the version-16 upgrade guide is the fastest index). The deltas we've actually hit and verified this session — keep this list current as more surface:

- **`params` / `searchParams` are Promises.** Route Handlers and pages receive them as promises — `await` them (`const { slug } = await props.params`). Verified in `.../upgrading/version-16.md`.
- **`cookies()` and `headers()` from `next/headers` are async.** `await` them before use.
- **`error.tsx` retry prop is `unstable_retry`, not `reset`.** (Also applies to `global-error.tsx` — see [ui.md](./ui.md).)
- **`next/image`: `priority` is deprecated → use `preload`,** and `next.config` must declare a `qualities` array (default is now only `[75]`; a `quality` prop outside the array is coerced to the nearest listed value).
- **`redirect()` must be called OUTSIDE try/catch** — it works by throwing `NEXT_REDIRECT`, so a surrounding catch will swallow it.

## Frontend / UI

The authenticated UI is **built**: shadcn on Base UI (`base-nova`), an icon-sidebar app shell, Google sign-in, and the error/404/loading conventions. Route protection is enforced in the `(app)` route-group layout (a Server Component), **not** middleware. Full details — component library, theming/tokens, the app shell, adding a nav item, and the Next 16 `unstable_retry` nuance — live in **[ui.md](./ui.md)** (and `.claude/rules/ui.md`).

## Admin area (localhost-only)

`src/app/admin/**` is a **local-only tooling surface** for data seeding/maintenance: two CSV importers — staff (`upload-staff`) and PTO (`upload-pto`) — plus a **bulk employment editor** (`bulk-edit-roles`) that maintains existing `staff_employment` rows (in-place correction or new effective-dated rows; the only in-app way to edit employment facts — see [ADR 0007](./decisions/0007-staff-employment-effective-dating.md)). Those three are staff-profiles tooling (see [staff-profiles.md](./domains/staff-profiles.md)). A fourth, **Manage Users** (`manage-users`), edits application users' RBAC role + ban status — see [permissions.md](./domains/permissions.md). A **Promote self to admin** button (`promote-self-button.tsx` → `promoteSelfToAdmin`) breaks the chicken-and-egg of that tool: it's the **one** place a role is set by a direct column write rather than the Better Auth admin API (whose `setRole` requires the caller to already be an admin), gated by `secureActionClient` + `assertLocalhost` so it can only ever promote the current user, locally. Two deliberate choices:

- **Outside the `(app)` route group, on purpose.** The `(app)` layout redirects users without an active staff record to `/profile-setup`; the staff-upload tool is exactly what *creates* those records (chicken-and-egg). So admin must NOT require auth/staff — it requires only that the request is local.
- **The security boundary is the host, not auth.** `src/lib/auth/admin.ts` exports `isLocalhost()` / `assertLocalhost()`, which check the request `host` header against loopback hosts (`localhost`, `127.0.0.1`, `::1`). `admin/layout.tsx` calls `isLocalhost()` and `notFound()`s the whole segment for non-local requests; **every admin action calls `assertLocalhost()` itself** (no middleware does it for them). The importers + bulk-editor use `publicActionClient` (local seeding must run before any staff/admin exists). The one exception is `commitUserChanges` (manage-users): it adds `secureActionClient` + `metadata({ role: "admin" })` because it mutates through the Better Auth admin API, whose endpoints require the *caller* to be an admin — so that tool also needs a signed-in admin (bootstrapping caveat in [permissions.md](./domains/permissions.md)). Enforced server-side only — never trusted from the client. Reachable by direct URL; there's no sidebar nav entry. See [ADR 0008](./decisions/0008-localhost-only-admin-area.md).

## Env

`src/env.ts` validates `process.env` once at import with zod. It is **server-only by convention** but deliberately omits `import "server-only"` — drizzle-kit and the better-auth CLI run in plain Node and import the module chain (db → env), so the marker would break them. Client code must read `NEXT_PUBLIC_*` directly. Optional vars use a preprocess that coerces **blank entries (`FOO=`) to `undefined`** rather than failing on a zero-length string. Required: `DATABASE_URL`, `BETTER_AUTH_SECRET`. Optional: `BETTER_AUTH_URL`, and `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` — both must be set for sign-in to work, since Google is now the only auth method. Real Google OAuth creds and a Neon-hosted Postgres `DATABASE_URL` are configured in `.env`; that DB is migrated and up to date.

## Auth

better-auth with the **admin** plugin — wired with the RBAC access controller and roles from `src/lib/auth/permissions.ts` (`admin({ ac, roles, adminRoles: ["admin"], defaultRole: "user" })`; see [domains/permissions.md](./domains/permissions.md), [ADR 0014](./decisions/0014-rbac-better-auth-access-control.md)); **Google-only sign-in** — `emailAndPassword` is disabled and Google is the sole social provider (see [decisions/0006](./decisions/0006-google-only-auth-and-layout-gating.md)). Server instance (`src/lib/auth/auth.ts`) uses the Drizzle adapter over our singleton `db`; **the better-auth tables (`user`, `session`, `account`, `verification`) live in our own schema/migrations** (`auth-schema.ts`), regenerated via `bun run auth:generate`. Two helpers gate the app:

- `getCurrentUser()` — reads the session, returns the user or `null`.
- `checkAuth(role)` — throws a `UserSafeActionError` if unauthenticated or under-privileged; **admins satisfy any role requirement** (admin override). Used by the secure action middleware.

`nextCookies()` must be the last plugin so Set-Cookie flushes from server actions. The whole auth API is mounted by one catch-all route (`src/app/api/auth/[...all]/route.ts`).

**Auth UI is built.** `/login` (in the `(auth)` group) shows a single "Continue with Google" button; the `(app)` route-group **layout** (`src/app/(app)/layout.tsx`, a Server Component) gates every authenticated page by calling `getCurrentUser()` and `redirect("/login")` when there's no session — **route protection is in the layout, not middleware** (consistent with the no-middleware posture). Sign-in via `authClient.signIn.social({ provider: "google" })`, sign-out via `authClient.signOut()`. See [ui.md](./ui.md) for the screens and [decisions/0006](./decisions/0006-google-only-auth-and-layout-gating.md) for the why. Layout gating guards navigation only — server actions still authorize independently (see Authorization below).

**Beyond a session, the app gates on a usable staff record.** After the session check, the `(app)` layout calls `getCurrentStaffAccess(user)` (`src/actions/staff/getCurrentStaffAccess.ts` — formerly `src/lib/staff.ts` `getCurrentStaff`, now in the actions layer per ADR 0010), which resolves the user to a `staff` row and returns a discriminated `StaffAccessStatus` (`ok` | `incomplete` | `not_setup`). Only `ok` (active staff record with ≥1 employment row) enters the app; the others redirect to the single `/profile-setup` block screen (`(onboarding)` route group, no group layout — the page self-gates). On first login it auto-links the email-synced staff row by writing `staff.userId` (guarded on `userId IS NULL`). See [flows.md](./flows.md) (auth flow), the [staff-profiles domain doc](./domains/staff-profiles.md), and [decisions/0006](./decisions/0006-google-only-auth-and-layout-gating.md).

## Data access — the actions layer is the only door to the DB

`db` is imported **only from `src/actions/**`**. Pages, layouts, and components — **including SSR Server Components** — never import `db` or query Drizzle directly; they call into the actions layer. This keeps every read and write in one place to authorize, project columns, and apply domain rules. See [ADR 0010](./decisions/0010-actions-layer-owns-db-access.md), `.claude/rules/server-actions.md`, `.claude/rules/database.md`.

- **Mutations** → next-safe-action actions (the action layer below / [ADR 0004](./decisions/0004-action-layer.md)).
- **Reads (incl. SSR)** → a plain **server-only** async function in the domain folder: `import "server-only"`, named `get<Thing>.ts` (e.g. `src/actions/staff/getStaffProfile.ts`, which `/staff/[id]` `await`s). **Not** a `'use server'` action — that would force the `{ data, serverError }` envelope and re-run session checks, awkward for SSR. It exports its return type. Personal reads resolve the user inside and filter by ownership (inherently scoped); **cross-person reads** (the staff directory and `/staff/[id]`) take an id and are **not** ownership-scoped — the `(app)` layout gate is their boundary, with capability checks layered where data is sensitive (e.g. `getStaffPto` requires `pto.review` for non-owners — see Authorization, [domains/permissions.md](./domains/permissions.md)).

**Legitimate `db` importers outside `actions/`:** framework wiring (the Better Auth Drizzle adapter in `src/lib/auth/auth.ts`) and pure compute helpers an action delegates to (`src/lib/*-import/plan.ts`, reached only through an action). The **synthetic-data seed** (`scripts/seed/**`, run via `bun run db:seed`) is a **deliberate exception**: as a one-off CLI it constructs and owns its own `postgres` client (`scripts/seed/client.ts`) so it can close the connection and exit, rather than reusing the app's long-lived singleton — see [development.md](./development.md#seeding-synthetic-data). (The former `getCurrentStaff`/`src/lib/staff.ts` straggler is **resolved** — it now lives in the actions layer as `getCurrentStaffAccess`.)

## Authorization — RBAC declared in action metadata

Most data is sensitive (rates, salaries, reviews), so authz is first-class. There's
a full **role-based access-control model** ([ADR 0014](./decisions/0014-rbac-better-auth-access-control.md),
[domains/permissions.md](./domains/permissions.md)) on Better Auth's native access
control, with `src/lib/auth/permissions.ts` as the single source of truth (the statement,
the roles, the role→permission matrix, and the `userHasPermission` / `requirePermission`
helpers). Authorization is **declared in action metadata, never hand-written in
action bodies**. There is **one** `secureActionClient`; its middleware runs all three
declarative forms, in order, *before* the body (see `.claude/rules/server-actions.md`
and `.claude/rules/permissions.md`):

1. **Coarse role** — `metadata.role` → `checkAuth(role)` (admin-override).
2. **Static capability** — `metadata.permission` (a `PermissionCheck`) →
   `requirePermission`. Use for capabilities that don't depend on the input.
3. **Input-dependent / ownership** — `metadata.authorize`, an **`ActionAuthorize`**
   hook (`{ user, clientInput } => void | Promise<void>`) that reads the raw
   `clientInput` (the target id, cross-field rules) and throws `UserSafeActionError`
   to deny. This is a **generic, reusable mechanism**, not staff-specific — any
   domain supplies its own hook. Mandatory wherever a target id could be acted on
   across users; a route-level gate alone is not enough.

`metadata.authorize` replaced the earlier "compose a bespoke client" approach — the
hook lives on the single `secureActionClient`, so there's no per-feature client to
build. (Reads are plain server-only functions, not actions — they call the helpers
inline, e.g. `getStaffPto` requiring `pto.review` for non-owners, or
`getProjectCostBasis` returning `null` for a viewer without `projects.viewMargin`
**before it queries `staff_employment` at all**. When a read ships to a client
component, the gate has to live in the read — hiding the value in the UI would still
put it in the payload. See [ADR 0053](./decisions/0053-project-budgets-and-margin.md).)

Helpers **fail closed** (unknown/null role → least privilege). The matrix is the
contract — asserted by `src/lib/auth/permissions.test.ts` (runs in `bun run check`) and
audited by `/audit-rbac`. **Never weaken or bypass a permission check; flag any gap
as a vulnerability** (`.claude/rules/permissions.md`).

The earlier open-staff-edit gap is **now closed** ([ADR 0014](./decisions/0014-rbac-better-auth-access-control.md)):
staff link/intro edits declare `metadata({ authorize: authorizeStaffEdit })`
(`src/actions/staff/canEditStaff.ts`), which gates via `canEditStaff` (own → always;
other → `staff.edit`), and `getStaffPto` requires `pto.review` to read another
person's PTO.

## Running the DB

The DB is not auto-provisioned. Dev uses a **remote Postgres** (e.g. Neon) — there is no local DB. Set `DATABASE_URL` (get it from the team), then `bun run db:migrate` to apply migrations. Iterate with `db:generate` → `db:migrate` (or `db:push` for quick dev), browse with `db:studio`. See `.claude/rules/database.md`.

## Cross-cutting concerns

- **Authorization** — see the metadata-driven RBAC model above and [domains/permissions.md](./domains/permissions.md). Role-based, capability-gated, not an afterthought.
- **Time** — allocations and timesheets are inherently time-ranged; date/timezone handling needs a deliberate approach.
- **Auditing** — financial and performance data likely need change history.

## Open questions

- Single-tenant (one consultancy) or multi-tenant? Assumed **single-tenant internal** until told otherwise.
- Billing: does this platform invoice, or just produce the billing basis for another system?
- Integrations: HR, accounting, calendars?
